# Архитектура: где что лежит

Файл отвечает на два вопроса: **что где живёт** и **как добавлять новое, ничего не забыв**.
Карта интерфейса ниже не декоративная: её проверяет `test/repo-shape.test.js` (тест
«раскладка интерфейса»). Забыли строку, тег в разметке или файл в списке для телефона —
тест падает, а не молчит.

## Слои

| Слой | Файлы | Роль |
|---|---|---|
| Точка входа главного процесса | `src/bootstrap.js` → `src/main.js` | окно Electron, IPC, инструменты агента |
| Мост в окно | `src/preload.js` | превращает IPC в `window.api` для окна |
| Окно (рендерер) | `src/renderer/*` | интерфейс: чат, панели, настройки |
| Сервисы главного процесса | остальные `src/*.js` | git, облако, деплой, почта, хранилища |
| Телефон | `src/mobile-bridge.js` | отдаёт те же файлы окна по LAN; список выдачи — `STATIC_FILES` там же |

Порядок загрузки скриптов окна задаёт `src/renderer/index.html`. Модули, от которых
зависит другой модуль, стоят **до** него: `provider-*.js` и данные ядра — до `agent-core.js`,
`agent-core.js` и панели — до `app.js`.

## Карта интерфейса

Третий столбец — «где это подключается»: `файл: фрагмент`, причём фрагмент обязан
реально быть в этом файле (тест это проверяет). `—` означает «никто не зовёт напрямую».

<!-- UI-MAP:START -->
| Файл | Что держит | Где подключается |
|---|---|---|
| src/renderer/index.html | разметка окна: чат, панели, настройки, модалки | — точка входа |
| src/renderer/styles.css | тема: токены, тёмная схема, раскладка | — |
| src/renderer/monochrome.css | монохромная схема (выбор в настройках) | — |
| src/renderer/boot-guard.js | дозор запуска: показывает поломку окна человеку (причина, файл, строка, версия кода) | index.html — первым скриптом: root.BootGuard |
| src/renderer/provider-config.js | адреса, ключи и заголовки провайдеров моделей | agent-core.js: root.ProviderConfig |
| src/renderer/provider-transport.js | транспорт провайдеров: диалекты, поток, окно модели | agent-core.js: root.ProviderTransport |
| src/renderer/context-window.js | бюджет контекста: оценка токенов, обрезка истории | agent-core.js: root.ContextWindow |
| src/renderer/web-tools.js | веб-инструменты агента (поиск, загрузка страниц) | agent-core.js: root.WebTools |
| src/renderer/image-tools.js | подготовка изображений для моделей | agent-core.js: root.ImageTools |
| src/renderer/prompts.js | системный промпт агента и правила ролей | agent-core.js: root.Prompts |
| src/renderer/tool-schemas.js | таблица схем инструментов (159 штук) | agent-core.js: root.ToolSchemas |
| src/renderer/agent-core.js | ядро агента: запросы, инструменты, план, память | app.js: window.AgentCore |
| src/renderer/markdown.js | рендер markdown и диаграмм без библиотек | chat-render.js: MdRender.render( |
| src/renderer/bootstrap.js | заглушка для телефона + страховочная загрузка field-guard | — |
| src/renderer/mobile-api.js | работа окна на телефоне: IPC через WebSocket | side-panel.js: window.mobileApi |
| src/renderer/yc-console.js | консоль Yandex Cloud в правой панели | yc-panel.js: window.YcConsole |
| src/renderer/yc-console.css | стили консоли Yandex Cloud | — |
| src/renderer/yc-panel.js | панель Yandex Cloud: дашборд и подключение в настройках | app.js: window.YcPanel({ |
| src/renderer/deploy-panel.js | панель деплоя: стадии, логи, откат | chat-events.js: window.DeployPanel |
| src/renderer/deploy-panel.css | стили панели деплоя | — |
| src/renderer/dev-run.js | быстрый запуск проекта: dev-сервер, порт, логи | side-panel.js: window.DevRun({ |
| src/renderer/chat-actions.js | удобство чата: копирование, регенерация, правка сообщения | app.js: window.ChatActions({ |
| src/renderer/web-chat.js | веб-режим: цикл чата в браузере без главного процесса | app.js: window.WebChat({ |
| src/renderer/tasks-mission.js | роли чата, дела и миссия: панели и их состояние | side-panel.js: window.TasksMission({ |
| src/renderer/secrets-panel.js | секреты: пароли сайтов, почта, переменные агента | app.js: window.SecretsPanel({ |
| src/renderer/highlight.js | подсветка кода для читалки файлов | project-panel.js: window.Highlight |
| src/renderer/qr.js | QR-код подключения телефона | mobile-panel.js: window.QR |
| src/renderer/mobile-panel.js | мобильный доступ: QR, статус моста, PIN, адреса для телефона | app.js: window.MobilePanel({ |
| src/renderer/chat-thinking.js | блок размышлений модели: сборка, автопрокрутка, свёртывание | app.js: window.ChatThinking() |
| src/renderer/chat-segments.js | сегменты ответа: лог «текст → действия → текст» | app.js: window.ChatSegments({ |
| src/renderer/plan-panel.js | план работ: чеклист, разбор плана из текста ответа, галочки | app.js: window.PlanPanel({ |
| src/renderer/chat-render.js | отрисовка сообщения: текст, вложения, метка времени, кнопки | app.js: window.ChatRender({ |
| src/renderer/chat-feed.js | лента: умная прокрутка и очередь кадра при стриме | app.js: window.ChatFeed({ |
| src/renderer/chat-work.js | строки действий агента и группа работ текущего ответа | app.js: window.ChatWork({ |
| src/renderer/chat-events.js | события агента: разбор `ai:event` и оверлеи картинки/диффа/предпросмотра | app.js: window.ChatEvents({ |
| src/renderer/openai-profiles.js | сохранённые OpenAI-подключения: список, выбор, удаление | app.js: window.OpenaiProfiles({ |
| src/renderer/settings-search.js | поиск по настройкам: фильтр по всем вкладкам | app.js: window.SettingsSearch({ |
| src/renderer/settings-panel.js | настройки: вкладки, поля, провайдеры, замер модели, сохранение | app.js: window.SettingsPanel({ |
| src/renderer/g4f-panel.js | выбор провайдера G4F: поиск, список, тест провайдера, подбор порта | app.js: window.G4fPanel({ |
| src/renderer/auto-tasks.js | автозадачи: чат «Автозадачи», очередь дел, запуск по сроку и «▶ сейчас» | app.js: window.AutoTasks({ |
| src/renderer/model-popup.js | быстрый выбор модели в шапке: попап, кэш списка, «↻ Обновить», клик мимо | app.js: window.ModelPopup({ |
| src/renderer/ask-modal.js | модалка «вопрос агента» (askUser): показ вопроса, ответ и отмена | app.js: window.AskModal({ |
| src/renderer/chat-rename.js | переименование чата: инлайн-ввод в заголовке по двойному клику | app.js: window.ChatRename({ |
| src/renderer/chat-store.js | хранилище: загрузка настроек и истории, чистка после сбоя, запись и автосохранение | app.js: window.ChatStore({ |
| src/renderer/chat-run.js | прогон ответа: признак прогона, остановка, завершение ответа, откат правок агента | app.js: window.ChatRun({ |
| src/renderer/chat-send.js | отправка: ход агента (история, сессия, стрим), обычная отправка и досылка прерванного ответа | app.js: window.ChatSend({ |
| src/renderer/chat-continue.js | продолжение чата: контекст задачи из прошлого чата и кнопка «Продолжить контекст» | app.js: window.ChatContinue({ |
| src/renderer/project-panel.js | панель проекта: файлы, правка, вкладки, коммиты, изменения, публикация | app.js: window.ProjectPanel({ |
| src/renderer/field-guard.js | страховка полей ввода (фокус не теряется) | — сам навешивает защиту |
| src/renderer/side-panel.js | правая панель и рельса: разделы, консоль рабочей папки, превью; здесь же собираются DevRun и TasksMission | app.js: window.SidePanel({ |
| src/renderer/command-palette.js | палитра команд: действия по разделам, поиск по файлам проекта, запуск | app.js: window.CommandPalette({ |
| src/renderer/app.js | оболочка окна: чат, панели, настройки, события | — собирает всё в окне |
<!-- UI-MAP:END -->

## Правила

### 1. Новый модуль окна (четыре шага, все обязательны)

1. Файл в `src/renderer/`, разметка подключения — по образцу `provider-transport.js`:
   фабрика `root.Xxx = factory`, зависимости приходят аргументами, наружу — только то,
   что реально нужно.
2. Тег `<script>` в `index.html` **до** того модуля, который его зовёт.
3. Имя файла в `STATIC_FILES` моста (`src/mobile-bridge.js`) — иначе телефон получит 404
   и раздел на телефоне молча перестанет работать.
4. Строка в карте выше. Всё это проверяет `repo-shape` (раскладка интерфейса).
5. **Зависимости проводки вычисляются на загрузке окна.** Если модуль объявлен НИЖЕ
   блока `window.Xxx({ ... })`, внутрь идёт отложенная стрелка (`getYcPanel: () => YcPanel`),
   а не значение: прямое чтение биндинга падает с «Cannot access before initialization»
   и обрывает всю оставшуюся загрузку (вместе с подпиской на события агента). Ловят
   это сторож `repo-shape` («проводки модулей не читают модуль, объявленный ниже»)   и живой прогон десктопа.

### 1а. Новый модуль бэкенда (вынос из `src/main.js`)

1. Файл в `src/`, фабрика `registerXxxIpc(deps)`; зависимости — аргументами, наружу — только
   нужное (например, клон-помощники, которые берёт ещё и инструмент агента).
2. **Живые значения — мостом `live` с getter/setter, а не копией:** окно создаётся позже
   сборки модуля (`window: () => mainWindow`), а общие флаги меняет не только он
   (`setLastAgentRepoDir`). Иначе событие уходит в `null` и раздел молча зависает.
3. **Проводка в `main.js` — на прежнем месте куска.** Если кусок зовут панели, собранные
   ниже, сборка модуля обязана стоять ВЫШЕ них: иначе в проводку панели придётся
   положить стрелку, а это уже подмена вызова, а не переименование.
4. Имя файла — в `backendSrc()` теста smoke и в двух списках `scanWiring` — иначе
   сторож «модуль ссылается на состояние main.js без внедрения» его не видит.
5. Строка в карте бэкенда ниже; `npm test` целиком зелёный.
6. **Перенос сверяется построчно с `HEAD`** (аудит `audit:extract` работает по окну):
   отличий должно быть ровно столько, сколько подстановок живого доступа.
7. **Живой прогон каналов**: `main.js` грузится в Node с поддельным `electron`, поэтому
   поведение проверяется по-настоящему — вплоть до сети и `git` (`test:live:env`,
   `test:live:model`, `test:live:github`). Плюс негативный контроль: сломать и убедиться,
   что прогон падает.

### 2. Вынос куска из большого файла (процесс, он же чек-лист приёмки)

1. **Анализ до правки**: точные границы (якоря), зависимости внутрь и наружу, цена в тестах.
2. Перенос **скриптом с проверкой якорей**: скрипт падает до записи, если якорь не совпал.
3. Текст переезжает **байт в байт**; меняется только доступ к общим данным
   (например, `settings.x` → `getSettings().x`, если модуль живёт дольше одной загрузки).
   **Вызовы в оболочке только ПЕРЕИМЕНОВЫВАЮТСЯ**: `scrollBottom()` →
   `ChatFeed.scrollBottom()`. Подмена вызова другим (даже похожим) — это изменение
   поведения, и оно идёт отдельным решением, а не по ходу выноса. Один такой случай
   уже был ценой пустого чата: `pinnedToBottom = true; scrollBottom();` заменили на
   `ChatFeed.jumpToBottom()`, который дополнительно трогал кнопку «↓» внутри ленты.
   Обратная поломка того же рода (1.5.157): вызов `setStreaming(false)` превратился в
   похожий `setStreamingFlag(false)` — состояние снималось, а ШАПКА не перерисовывалась,
   и в окне навсегда оставались крутилка и «Стоп» вместо «Отправить». Отсюда правило:
   **если функция меняет и состояние, и разметку, вынос обязан звать ИМЕННО её, а не её
   половинку.** Ловится это только живым прогоном окна (пункт 6), поэтому такой прогон
   теперь есть в песочнице.
4. Перенос **проверяется аудитом**: `bun run audit:extract <до> <после> <модули...>`
   раскладывает ушедшие из оболочки строки на «перенесено как есть», «перенесено
   с живым доступом» (`settings.x` → `getSettings().x`, `currentPreset` → `getPreset()`,
   `sbVersion = v` → `setSbVersion(v)`, `MobilePanel.x` → `getMobilePanel().x`),
   «переименование вызова» и «требует глаз»; последние объясняются вручную.
   Вместо ревизии «после» можно указать `WORKTREE` — аудит до коммита. Каждая замена нескольких
   строк одним вызовом (например `ensureWorkGroup(); body.appendChild(el)` →
   `ChatWork.addRow(el)`) должна быть сверена по коду модуля.
5. Один этап = один заход, `npm test` целиком зелёный после него.
6. Живая проверка: HTTP моста + запуск скриптов в окне в порядке тегов (в песочнице) и
   `bun run test:live` на ПК. Отдельно — живой прогон **через окно** (ввод → «Отправить»):
   прежние наборы водили работу через `window.api` и потому не видели поломок ленты.
   С 1.5.157 `bun run test:boot` сам прогоняет полный цикл ответа в окне (щелчок по
   «Отправить» → прогон → завершение, включая ошибку провайдера) и проверяет, что шапка
   вернулась. Живой `test:live` на ПК перед каждым разделом теперь ждёт `#btn-send`
   (`waitIdle`) и называет причину вместо таймаута в 30 с.
7. OTA пересобран, файлы бандла сверены с диском, версия поднята, заметка в `AGENT-NOTES.md`.

### 3. Как тесты читают текст интерфейса

- `uiFile("app.js")` — конкретный файл; `uiAll()` — весь интерфейс; `uiFind("маркер")` /
  `uiFind("маркер", "конец")` — кусок кода, где бы он ни лежал (после выноса тест не падает).
- **Маркер конца выбирайте внутри самого куска.** Если кусок уехал в модуль, а маркер
  конца остался в `app.js` (например это комментарий *следующей* секции), `uiFind` не
  угадывает — он падает и называет файл и маркер. При выносе это правится одной строкой:
  конец сужается до строки, которая гарантированно уезжает вместе с куском.
- Кто проверяет, что в `app.js` чего-то **больше нет**, читает `app.js` напрямую — это нарочно.
  Но проверка «функция X есть в `app.js`» при выносе X в модуль должна смотреть в модуль
  (или в `uiAll()`): тест падает громко и называет, что именно править.
- Прямых чтений `app.js` в тестах ограниченное число, и оно не должно расти
  (тест «бюджет прямых чтений app.js»; сейчас 34 — предел 34: у каждого набора вынесенного
  модуля ровно одно, разбор окна закончен — дальше читать app.js тестами не придётся). Одно чтение на модуль
  допустимо: это проверка «его кода в оболочке больше нет». Всё остальное — через
  `uiAll()`/`uiFile()`.

### 4. Признаки, что из `app.js` пора выносить

Файл больше 1500 строк, или в нём появилась новая панель, или кусок зовёт наружу больше
десяти имён. Порядок выноса — по возрастанию связности, а не по размеру: сначала то,
у чего на входе 5–10 зависимостей (как `yc-panel.js`), в конце — то, на чём висит весь чат.

### 5. Обновления (OTA): две шкалы версий

- **Версия приложения** (`package.json`) — версия кода. **Номер набора** (`ota/manifest.json`)
  — версия сборки. Это разные шкалы: набор 1.5.125 может быть собран из кода 1.5.114.
- Набор несёт версию кода внутри себя (`manifest.codeVersion`, при применении —
  `version.json`), и приложение решает, грузить ли применённый набор, **по версии кода**:
  набор старше кода в папке — не перекрывает его и удаляется. Судить по номеру набора
  нельзя: номера уходят вперёд, и после `git pull` приложение продолжало запускать старый
  код (ровно жалоба «обновил папку, а версия та же»).
- Поэтому **после обновления кода номер набора должен быть не ниже версии приложения**, а
  лучше — равен ей (`node scripts/make-ota.js --version <версия>`). Панель Self-update
  показывает версию кода и номер набора раздельно, а также все источники и доступное
  обновление — по ней всегда видно, что именно исполняется.

## Карта бэкенда (справочно, тестом не проверяется)

| Файл | Роль |
|---|---|
| `src/main.js` | главный процесс: окно, IPC, чат-цикл, инструменты (40 обработчиков) |
| `src/agent-tools.js` | 159 обработчиков инструментов агента (один `createAgentTools(deps)`) |
| `src/browser-tools.js` | браузер агента: карта страницы, действия, replay, догрузка |
| `src/agent-store.js` | заметки, чекпоинты, памятки, дела, повторы (папка `.agent/`) |
| `src/yandex-cloud.js`, `yc-*.js` | Yandex Cloud: статус, ресурсы, логи, стоимость, CLI, консоль |
| `src/deploy-*.js`, `cloud-state.js` | деплой: рецепты, состояние, конвейер, проверки |
| `src/mail.js`, `mail-ipc.js` | почта SMTP/IMAP |
| `src/git-ipc.js`, `fs-ipc.js`, `system-stack.js` | git, файлы проекта, стек и окружение |
| `src/mission-ipc.js`, `mission-store.js` | дела, миссии и папка работы агента: каналы `tasks:*`, `mission:*`, `agentfiles:*` |
| `src/model-ipc.js` | модели, замер локальной модели и G4F: каналы `ai:models`, `ai:probeLocal`, `g4f:probe`, `g4f:test` |
| `src/github-ipc.js` | GitHub: вход по коду, список и поиск репозиториев, выбор и клонирование, публикация нового репозитория: каналы `github:*` |
| `src/settings-store.js` | схема настроек и миграции, чтение и запись (секреты — отдельно), OpenAI-подключения с кулдауном, история чатов: `loadSettings`/`saveSettings`/`loadChats`/`saveChats` |
| `src/bg-processes.js` | фоновые процессы и постоянные оболочки (`bgSpawn`/`bgKill`, кольцевой буфер вывода, остановка вместе с деревом), распознавание dev-команд (`SERVER_CMD_RE`), освобождение порта (`killProcessesOnPort`) и HTTP-проверка адреса (`checkUrlStatus`) |
| `src/undo-store.js` | снимки отката правок агента (`snapshotFileForUndo` с вытеснением на файл) и чекпоинт на диске (`persistUndo`/`loadPersistedUndo`, `undoFile`) |
| `src/project-search.js` | обход проекта (`walkProject`), список файлов для «осмотра» (`listProjectFiles`) и поиск по содержимому (`searchProjectFiles`) |
| `src/paths-git.js` | пути и git для всего бэкенда: `resolvePath`/`sanitizeDir`/`sanitizePath`, `gitDirOrHome`/`agentWorkDir`, `repoNameFromUrl`/`stripUrlCreds`, `runGit` (окружение по назначению + Basic-авторизация) |
| `src/app-ui-tools.js`, `tool-policy.js` | интерфейсные инструменты, разрешения |
| `src/mobile-bridge.js`, `secrets.js`, `ota.js`, `vault.js` | телефон, секреты, обновления, пароли |
