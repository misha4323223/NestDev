# Архитектура: где что лежит

Файл отвечает на два вопроса: **что где живёт** и **как добавлять новое, ничего не забыв**.
Карта интерфейса ниже не декоративная: её проверяет `test/repo-shape.test.js` (тест
«раскладка интерфейса»). Забыли строку, тег в разметке или файл в списке для телефона —
тест падает, а не молчит.

## Слои

| Слой | Файлы | Роль |
|---|---|---|
| Точка входа главного процесса | `src/bootstrap.js` → `src/main.js` | окно Electron, IPC, инструменты агента |
| Мост в окно | `src/preload.js` | превращает IPC в `window.api` для окна |
| Окно (рендерер) | `src/renderer/*` | интерфейс: чат, панели, настройки |
| Сервисы главного процесса | остальные `src/*.js` | git, облако, деплой, почта, хранилища |
| Телефон | `src/mobile-bridge.js` | отдаёт те же файлы окна по LAN; список выдачи — `STATIC_FILES` там же |

Порядок загрузки скриптов окна задаёт `src/renderer/index.html`. Модули, от которых
зависит другой модуль, стоят **до** него: `provider-*.js` и данные ядра — до `agent-core.js`,
`agent-core.js` и панели — до `app.js`.

## Карта интерфейса

Третий столбец — «где это подключается»: `файл: фрагмент`, причём фрагмент обязан
реально быть в этом файле (тест это проверяет). `—` означает «никто не зовёт напрямую».

<!-- UI-MAP:START -->
| Файл | Что держит | Где подключается |
|---|---|---|
| src/renderer/index.html | разметка окна: чат, панели, настройки, модалки | — точка входа |
| src/renderer/styles.css | тема: токены, тёмная схема, раскладка | — |
| src/renderer/monochrome.css | монохромная схема (выбор в настройках) | — |
| src/renderer/boot-guard.js | дозор запуска: показывает поломку окна человеку (причина, файл, строка, версия кода) | index.html — первым скриптом: root.BootGuard |
| src/renderer/provider-config.js | адреса, ключи и заголовки провайдеров моделей | agent-core.js: root.ProviderConfig |
| src/renderer/provider-transport.js | транспорт провайдеров: диалекты, поток, окно модели | agent-core.js: root.ProviderTransport |
| src/renderer/context-window.js | бюджет контекста: оценка токенов, обрезка истории | agent-core.js: root.ContextWindow |
| src/renderer/web-tools.js | веб-инструменты агента (поиск, загрузка страниц) | agent-core.js: root.WebTools |
| src/renderer/image-tools.js | подготовка изображений для моделей | agent-core.js: root.ImageTools |
| src/renderer/prompts.js | системный промпт агента и правила ролей | agent-core.js: root.Prompts |
| src/renderer/tool-schemas.js | таблица схем инструментов (159 штук) | agent-core.js: root.ToolSchemas |
| src/renderer/agent-core.js | ядро агента: запросы, инструменты, план, память | app.js: window.AgentCore |
| src/renderer/markdown.js | рендер markdown и диаграмм без библиотек | chat-render.js: MdRender.render( |
| src/renderer/bootstrap.js | заглушка для телефона + страховочная загрузка field-guard | — |
| src/renderer/mobile-api.js | работа окна на телефоне: IPC через WebSocket | side-panel.js: window.mobileApi |
| src/renderer/yc-console.js | консоль Yandex Cloud в правой панели | yc-panel.js: window.YcConsole |
| src/renderer/yc-console.css | стили консоли Yandex Cloud | — |
| src/renderer/yc-panel.js | панель Yandex Cloud: дашборд и подключение в настройках | app.js: window.YcPanel({ |
| src/renderer/deploy-panel.js | панель деплоя: стадии, логи, откат | chat-events.js: window.DeployPanel |
| src/renderer/deploy-panel.css | стили панели деплоя | — |
| src/renderer/dev-run.js | быстрый запуск проекта: dev-сервер, порт, логи | side-panel.js: window.DevRun({ |
| src/renderer/chat-actions.js | удобство чата: копирование, регенерация, правка сообщения | app.js: window.ChatActions({ |
| src/renderer/web-chat.js | веб-режим: цикл чата в браузере без главного процесса | app.js: window.WebChat({ |
| src/renderer/tasks-mission.js | роли чата, дела и миссия: панели и их состояние | side-panel.js: window.TasksMission({ |
| src/renderer/secrets-panel.js | секреты: пароли сайтов, почта, переменные агента | app.js: window.SecretsPanel({ |
| src/renderer/highlight.js | подсветка кода для читалки файлов | project-panel.js: window.Highlight |
| src/renderer/qr.js | QR-код подключения телефона | mobile-panel.js: window.QR |
| src/renderer/mobile-panel.js | мобильный доступ: QR, статус моста, PIN, адреса для телефона | app.js: window.MobilePanel({ |
| src/renderer/chat-thinking.js | блок размышлений модели: сборка, автопрокрутка, свёртывание | app.js: window.ChatThinking() |
| src/renderer/chat-segments.js | сегменты ответа: лог «текст → действия → текст» | app.js: window.ChatSegments({ |
| src/renderer/plan-panel.js | план работ: чеклист, разбор плана из текста ответа, галочки | app.js: window.PlanPanel({ |
| src/renderer/chat-render.js | отрисовка сообщения: текст, вложения, метка времени, кнопки | app.js: window.ChatRender({ |
| src/renderer/chat-feed.js | лента: умная прокрутка и очередь кадра при стриме | app.js: window.ChatFeed({ |
| src/renderer/chat-work.js | строки действий агента и группа работ текущего ответа | app.js: window.ChatWork({ |
| src/renderer/chat-events.js | события агента: разбор `ai:event` и оверлеи картинки/диффа/предпросмотра | app.js: window.ChatEvents({ |
| src/renderer/openai-profiles.js | сохранённые OpenAI-подключения: список, выбор, удаление | app.js: window.OpenaiProfiles({ |
| src/renderer/settings-search.js | поиск по настройкам: фильтр по всем вкладкам | app.js: window.SettingsSearch({ |
| src/renderer/settings-panel.js | настройки: вкладки, поля, провайдеры, замер модели, сохранение | app.js: window.SettingsPanel({ |
| src/renderer/g4f-panel.js | выбор провайдера G4F: поиск, список, тест провайдера, подбор порта | app.js: window.G4fPanel({ |
| src/renderer/auto-tasks.js | автозадачи: чат «Автозадачи», очередь дел, запуск по сроку и «▶ сейчас» | app.js: window.AutoTasks({ |
| src/renderer/model-popup.js | быстрый выбор модели в шапке: попап, кэш списка, «↻ Обновить», клик мимо | app.js: window.ModelPopup({ |
| src/renderer/ask-modal.js | модалка «вопрос агента» (askUser): показ вопроса, ответ и отмена | app.js: window.AskModal({ |
| src/renderer/chat-rename.js | переименование чата: инлайн-ввод в заголовке по двойному клику | app.js: window.ChatRename({ |
| src/renderer/chat-store.js | хранилище: загрузка настроек и истории, чистка после сбоя, запись и автосохранение | app.js: window.ChatStore({ |
| src/renderer/chat-run.js | прогон ответа: признак прогона, остановка, завершение ответа, откат правок агента | app.js: window.ChatRun({ |
| src/renderer/chat-send.js | отправка: ход агента (история, сессия, стрим), обычная отправка и досылка прерванного ответа | app.js: window.ChatSend({ |
| src/renderer/chat-continue.js | продолжение чата: контекст задачи из прошлого чата и кнопка «Продолжить контекст» | app.js: window.ChatContinue({ |
| src/renderer/project-panel.js | панель проекта: файлы, правка, вкладки, коммиты, изменения, публикация | app.js: window.ProjectPanel({ |
| src/renderer/field-guard.js | страховка полей ввода (фокус не теряется) | — сам навешивает защиту |
| src/renderer/side-panel.js | правая панель и рельса: разделы, консоль рабочей папки, превью; здесь же собираются DevRun и TasksMission | app.js: window.SidePanel({ |
| src/renderer/command-palette.js | палитра команд: действия по разделам, поиск по файлам проекта, запуск | app.js: window.CommandPalette({ |
| src/renderer/app.js | оболочка окна: чат, панели, настройки, события | — собирает всё в окне |
<!-- UI-MAP:END -->

## Правила

### 1. Новый модуль окна (четыре шага, все обязательны)

1. Файл в `src/renderer/`, разметка подключения — по образцу `provider-transport.js`:
   фабрика `root.Xxx = factory`, зависимости приходят аргументами, наружу — только то,
   что реально нужно.
2. Тег `<script>` в `index.html` **до** того модуля, который его зовёт.
3. Имя файла в `STATIC_FILES` моста (`src/mobile-bridge.js`) — иначе телефон получит 404
   и раздел на телефоне молча перестанет работать.
4. Строка в карте выше. Всё это проверяет `repo-shape` (раскладка интерфейса).
5. **Зависимости проводки вычисляются на загрузке окна.** Если модуль объявлен НИЖЕ
   блока `window.Xxx({ ... })`, внутрь идёт отложенная стрелка (`getYcPanel: () => YcPanel`),
   а не значение: прямое чтение биндинга падает с «Cannot access before initialization»
   и обрывает всю оставшуюся загрузку (вместе с подпиской на события агента). Ловят
   это сторож `repo-shape` («проводки модулей не читают модуль, объявленный ниже»)   и живой прогон десктопа.

### 1а. Новый модуль бэкенда (вынос из `src/main.js`)

1. Файл в `src/`, фабрика `registerXxxIpc(deps)`; зависимости — аргументами, наружу — только
   нужное (например, клон-помощники, которые берёт ещё и инструмент агента).
2. **Живые значения — мостом `live` с getter/setter, а не копией:** окно создаётся позже
   сборки модуля (`window: () => mainWindow`), а общие флаги меняет не только он
   (`setLastAgentRepoDir`). Иначе событие уходит в `null` и раздел молча зависает.
3. **Проводка в `main.js` — на прежнем месте куска.** Если кусок зовут панели, собранные
   ниже, сборка модуля обязана стоять ВЫШЕ них: иначе в проводку панели придётся
   положить стрелку, а это уже подмена вызова, а не переименование.
4. Имя файла — в `backendSrc()` теста smoke и в двух списках `scanWiring` — иначе
   сторож «модуль ссылается на состояние main.js без внедрения» его не видит.
5. Строка в карте бэкенда ниже; `npm test` целиком зелёный.
6. **Перенос сверяется построчно с `HEAD`** (аудит `audit:extract` работает по окну):
   отличий должно быть ровно столько, сколько подстановок живого доступа.
7. **Живой прогон каналов**: `main.js` грузится в Node с поддельным `electron`, поэтому
   поведение проверяется по-настоящему — вплоть до сети и `git` (`test:live:env`,
   `test:live:model`, `test:live:github`). Плюс негативный контроль: сломать и убедиться,
   что прогон падает.

### 2. Вынос куска из большого файла (процесс, он же чек-лист приёмки)

1. **Анализ до правки**: точные границы (якоря), зависимости внутрь и наружу, цена в тестах.
2. Перенос **скриптом с проверкой якорей**: скрипт падает до записи, если якорь не совпал.
3. Текст переезжает **байт в байт**; меняется только доступ к общим данным
   (например, `settings.x` → `getSettings().x`, если модуль живёт дольше одной загрузки).
   **Вызовы в оболочке только ПЕРЕИМЕНОВЫВАЮТСЯ**: `scrollBottom()` →
   `ChatFeed.scrollBottom()`. Подмена вызова другим (даже похожим) — это изменение
   поведения, и оно идёт отдельным решением, а не по ходу выноса. Один такой случай
   уже был ценой пустого чата: `pinnedToBottom = true; scrollBottom();` заменили на
   `ChatFeed.jumpToBottom()`, который дополнительно трогал кнопку «↓» внутри ленты.
   Обратная поломка того же рода (1.5.157): вызов `setStreaming(false)` превратился в
   похожий `setStreamingFlag(false)` — состояние снималось, а ШАПКА не перерисовывалась,
   и в окне навсегда оставались крутилка и «Стоп» вместо «Отправить». Отсюда правило:
   **если функция меняет и состояние, и разметку, вынос обязан звать ИМЕННО её, а не её
   половинку.** Ловится это только живым прогоном окна (пункт 6), поэтому такой прогон
   теперь есть в песочнице.
4. Перенос **проверяется аудитом**: `bun run audit:extract <до> <после> <модули...>`
   раскладывает ушедшие из оболочки строки на «перенесено как есть», «перенесено
   с живым доступом» (`settings.x` → `getSettings().x`, `currentPreset` → `getPreset()`,
   `sbVersion = v` → `setSbVersion(v)`, `MobilePanel.x` → `getMobilePanel().x`),
   «переименование вызова» и «требует глаз»; последние объясняются вручную.
   Вместо ревизии «после» можно указать `WORKTREE` — аудит до коммита. Каждая замена нескольких
   строк одним вызовом (например `ensureWorkGroup(); body.appendChild(el)` →
   `ChatWork.addRow(el)`) должна быть сверена по коду модуля.
5. Один этап = один заход, `npm test` целиком зелёный после него.
6. Живая проверка: HTTP моста + запуск скриптов в окне в порядке тегов (в песочнице) и
   `bun run test:live` на ПК. Отдельно — живой прогон **через окно** (ввод → «Отправить»):
   прежние наборы водили работу через `window.api` и потому не видели поломок ленты.
   С 1.5.157 `bun run test:boot` сам прогоняет полный цикл ответа в окне (щелчок по
   «Отправить» → прогон → завершение, включая ошибку провайдера) и проверяет, что шапка
   вернулась. Живой `test:live` на ПК перед каждым разделом теперь ждёт `#btn-send`
   (`waitIdle`) и называет причину вместо таймаута в 30 с.
7. OTA пересобран, файлы бандла сверены с диском, версия поднята, заметка в `AGENT-NOTES.md`.

### 3. Как тесты читают текст интерфейса

- `uiFile("app.js")` — конкретный файл; `uiAll()` — весь интерфейс; `uiFind("маркер")` /
  `uiFind("маркер", "конец")` — кусок кода, где бы он ни лежал (после выноса тест не падает).
- **Маркер конца выбирайте внутри самого куска.** Если кусок уехал в модуль, а маркер
  конца остался в `app.js` (например это комментарий *следующей* секции), `uiFind` не
  угадывает — он падает и называет файл и маркер. При выносе это правится одной строкой:
  конец сужается до строки, которая гарантированно уезжает вместе с куском.
- Кто проверяет, что в `app.js` чего-то **больше нет**, читает `app.js` напрямую — это нарочно.
  Но проверка «функция X есть в `app.js`» при выносе X в модуль должна смотреть в модуль
  (или в `uiAll()`): тест падает громко и называет, что именно править.
- Прямых чтений `app.js` в тестах ограниченное число, и оно не должно расти
  (тест «бюджет прямых чтений app.js»; сейчас 34 — предел 34: у каждого набора вынесенного
  модуля ровно одно, разбор окна закончен — дальше читать app.js тестами не придётся). Одно чтение на модуль
  допустимо: это проверка «его кода в оболочке больше нет». Всё остальное — через
  `uiAll()`/`uiFile()`.

### 4. Признаки, что из `app.js` пора выносить

Файл больше 1500 строк, или в нём появилась новая панель, или кусок зовёт наружу больше
десяти имён. Порядок выноса — по возрастанию связности, а не по размеру: сначала то,
у чего на входе 5–10 зависимостей (как `yc-panel.js`), в конце — то, на чём висит весь чат.

### 5. Обновления (OTA): две шкалы версий

- **Версия приложения** (`package.json`) — версия кода. **Номер набора** (`ota/manifest.json`)
  — версия сборки. Это разные шкалы: набор 1.5.125 может быть собран из кода 1.5.114.
- Набор несёт версию кода внутри себя (`manifest.codeVersion`, при применении —
  `version.json`), и приложение решает, грузить ли применённый набор, **по версии кода**:
  набор старше кода в папке — не перекрывает его и удаляется. Судить по номеру набора
  нельзя: номера уходят вперёд, и после `git pull` приложение продолжало запускать старый
  код (ровно жалоба «обновил папку, а версия та же»).
- Поэтому **после обновления кода номер набора должен быть не ниже версии приложения**, а
  лучше — равен ей (`node scripts/make-ota.js --version <версия>`). Панель Self-update
  показывает версию кода и номер набора раздельно, а также все источники и доступное
  обновление — по ней всегда видно, что именно исполняется.

## Карта бэкенда (справочно, тестом не проверяется)

| Файл | Роль |
|---|---|
| `src/main.js` | главный процесс: чат-цикл и ПРОВОДКА — каналы `ipcMain.handle` регистрируют модули `registerXxxIpc`, своих обработчиков в оболочке нет (остался только прокси `ipcHandlerMap`, которым телефон получает те же обработчики, что окно); жизненный цикл вынесен в `src/lifecycle.js` (часть 37), `fetchModels` оставлен здесь как шов для живых прогонов |
| `src/lifecycle.js` | жизненный цикл приложения (`createLifecycle` → `startLifecycle`): старт (`whenReady`: окно, настройки мобильного моста, подписка апдейтера, тик OTA через 5 с и далее каждые 60 с, повторное окно по `activate`) и ПОРЯДОК остановки (`before-quit`: браузер агента → фоновые процессы → терминал → превью проекта → мост телефона; `window-all-closed` выходит везде, кроме macOS). Каждый шаг идёт через `runStep`: отказ шага виден в логе, но НЕ отменяет остальные — иначе мост с негодным портом тихо выключал проверку обновлений, а упавший шаг остановки оставлял dev-сервер держать порт |
| `src/agent-tools.js` | реестр инструментов агента (один `createAgentTools(deps)`): файлы, терминал, браузер, память и дела, миссии, система, сеть. Облачные инструменты (девять `yc*`) вынесены в `src/agent-tools-cloud.js` и подмешиваются сюда `...createCloudTools(deps)`, git и GitHub (шестнадцать) — в `src/agent-tools-git.js` и стоят ССЫЛКАМИ `"имя": git.имя,` на своих прежних местах (часть 40) — наружу реестр отдаёт те же 159 имён в том же порядке. Чтение и поиск файлов (десять: `readFile`…`findReferences`) — в `src/agent-tools-files.js`, тоже ССЫЛКАМИ `files.имя` на прежних местах (часть 40, заход 3a); запись и правки (`createFolder`, `writeFile`, `editFile`, `applyPatch`, `undoEdit`, `refactorRename`) — в `src/agent-tools-write.js`, ссылками `write.имя` (заход 3b); команды, оболочка и фоновые процессы (15: `runCommand`…`retryCommand`) — в `src/agent-tools-run.js`, ссылками `run.имя` (заход 4); система и установка ПО (девять: `getSystemInfo`…`installExe`) — в `src/agent-tools-system.js`, ссылками `system.имя` (заход 5); сеть и проверки доступности (семь: `checkUrl`…`downloadAndExtract`) — в `src/agent-tools-net.js`, ссылками `netTools.имя` (заход 6a); пояс проекта (пятнадцать: `noteSave`…`semanticSearch`) — в `src/agent-tools-memory.js`, ссылками `memory.имя` (заход 6b); миссии и план (пять: `missionStart`…`todoWrite`) — в `src/agent-tools-mission.js`, ссылками `mission.имя` (заход 6c); окно приложения, буфер обмена, скриншот экрана и открытие пути (двенадцать: `appRead`…`openPath`) — в `src/agent-tools-app.js`, ссылками `appTools.имя` (заход 8); вложения и разработка (тринадцать: `dockerBuild`…`dbQuery`) — в `src/agent-tools-devtools.js`, ссылками `devtools.имя` (заход 9); медиа, просмотр и справка (десять: `findTools`, `openUrl`, `showImage`…`screenshotCapture`, `waitForIdle`, `agentGuide`) — в `src/agent-tools-media.js`, ссылками `media.имя` (заход 10); пароли сайтов и почта (пять: `vaultList`, `vaultFill`, `mailSend`, `mailList`, `mailCode`) — в `src/agent-tools-vault.js`, ссылками `vaultTools.имя` (заход 11) |
| `src/agent-tools-git.js` | инструменты агента по git и GitHub: клон и публикация (`gitClone`, `gitPublish`), состояние и история (`gitStatus`, `gitDiff`, `gitLog`, `gitBlame`, `gitUndoLastCommit`, `gitRevert`), коммит и ветки (`gitCommit`, `gitPush`, `gitInit`, `gitBranch`, `gitCheckout`, `gitStash`, `gitCherryPick`). Собирается один раз (`createGitTools(deps, live)`) и получает живой мост вторым аргументом — `gitClone` пишет папку последнего клона и флаг «после клона» теми же сеттерами, что и остальные инструменты. Тела перенесены байт в байт; порядок инструментов в реестре сохранён ссылками. Живёт в обычном Node (`npm run test:live:tools-git` гоняет эти обработчики через настоящий реестр и настоящий git). Вынесено из `agent-tools.js` в части 40 (заход 2) |
| `src/agent-tools-cloud.js` | облачные инструменты агента (Yandex Cloud): визитка и разрешения (`ycStatus`), список ресурсов (`ycList`), создание (`ycCreate`), ориентир цены (`ycCosts`), удаление (`ycDelete`), выкладка проекта (`ycDeploy`), контейнеры и ревизии (`ycContainer`), логи (`ycLogs`), встроенный `yc CLI` (`ycInstall`). Всё нужное приходит тем же плоским `deps`, что и в `agent-tools.js`, поэтому проводка в `main.js` не менялась; живого моста `live` модуль не требует и просто собирается в обычном Node (`npm run test:live:tools-cloud` гоняет эти обработчики через настоящий реестр и подменённое облако). Тела перенесены байт в байт. Вынесено из `agent-tools.js` в части 40 (заход 1) |
| `src/agent-tools-files.js` | файловые инструменты агента — чтение и разбор: `readFile` (включая `agent-guide:`), `readFileLines`, `fileOutline`, `listFiles`, `listDirectory`, `searchProject`, `searchFile`, `readFileStructure`, `explainCode`, `findReferences`. Помощники (`numberedLines`, `langFromExt`, `buildFileOutline`, `buildBlockRanges`, `findSymbolReferences`, `listProjectFiles`…) приходят тем же плоским `deps`, что и в `agent-tools.js`. Живого моста модулю не нужно: ни один обработчик не читает и не пишет состояние прогона — поэтому он собирается в обычном Node. Тела перенесены байт в байт; порядок инструментов в реестре сохранён ссылками. Живой прогон — `npm run test:live:tools-files` (36 проверок через настоящий реестр и настоящие файлы). Вынесено из `agent-tools.js` в части 40 (заход 3a). Запись и правки (`writeFile`, `editFile`, `applyPatch`, `undoEdit`…) остаются в реестре до захода 3b — им нужны откат правок и проверка записанного файла |
| `src/agent-tools-write.js` | файловые инструменты агента — запись и правки: `createFolder`, `writeFile` (проверка синтаксиса/JSON сразу после записи), `editFile` (точная замена с `occurrence`/`replaceAll` либо по номерам строк), `applyPatch` (unified diff), `undoEdit` (откат по журналу) и `refactorRename` (переименование по проекту). Здесь же помощники `checkWrittenFile`/`envNoteFor` с таблицей `CHECK_CODE_EXT` — ими пользуется только `writeFile`. Живой мост нужен ровно одному инструменту: `undoEdit` читает и пишет `live.activeRunUndo`/`live.lastUndoLog` — журнал отката живёт в main.js, и копия означала бы «откат не видит правок прогона». Тела перенесены байт в байт (кроме одной строки `undoEdit` — исправленный `activeRunUndo is not defined`, см. `AGENT-NOTES`), порядок инструментов сохранён ссылками. Живой прогон — `npm run test:live:tools-write` (61 проверка через настоящий реестр и настоящие файлы). Вынесено из `agent-tools.js` в части 40 (заход 3b) |
| `src/agent-tools-net.js` | инструменты агента — сеть и проверки доступности: `checkUrl` (статус, тип, начало тела), `checkPort`/`listPorts` (кто слушает порт и весь живой список слушателей), `webSearch`/`webFetch` (поиск и страница текстом), `apiRequest` (метод, заголовки, тело, таймаут 30 с, бинарное тело не показывается), `downloadAndExtract` (скачать архив и распаковать в папку проекта). Помощники приходят 11 именами в `deps` (`net-guard`, `checkUrlStatus`, `webSearchPage`/`webFetchPage`, `downloadAndExtractTo`), живого моста не нужно. Тела перенесены байт в байт, порядок инструментов сохранён ссылками `netTools.имя` (имя именно `netTools`: `net` занято распакованным модулем node). Живой прогон — `npm run test:live:tools-net` (35 проверок против НАСТОЯЩЕГО локального сервера: JSON-эхо запроса, HTML, живой tar.gz, не-архив, закрытый порт). Вынесено из `agent-tools.js` в части 40 (заход 6a) |
| `src/agent-tools-memory.js` | инструменты агента — пояс проекта (память работы между сессиями): заметки проекта (`noteSave`/`noteRead`/`noteList`/`noteDelete` — кириллический ключ переводится в латиницу сам, файл лежит в `userData/project-memory`, ВНЕ проекта, чтобы не попасть в git), дела со сроками (`taskAdd`/`taskList`/`taskUpdate`/`taskDone`/`taskDelete` — повтор «каждый день», автозапуск, отсрочка; каждая правка сразу отзывается панели через `emitTasksChanged`) и памятки диалогов (`memoryList`/`memorySearch` — дни и поиск по сжатым памяткам; галочка «Память диалогов» читается в МОМЕНТ вызова, иначе включение не подхватится без перезапуска). Точки отката: `checkpointSave` (снимок текстовых файлов рабочей папки), `checkpointList`, `checkpointRollback` (восстанавливает файлы на диске, новые не трогает). `semanticSearch` — поиск по смыслу по настоящему индексу (`src/code-index.js`). Помощники приходят 9 именами в `deps` (`agentStore`, `codeIndex`, `app`, `agentWorkDir`, `userDataDir`, `emitTasksChanged`, `loadSettings`, `resolvePath`, `fs`; `path` не нужен — разбор имени на стороне хранилища и `resolvePath`), живого моста не нужно — модуль собирается РОВНО с одним аргументом. Тела перенесены байт в байт, порядок инструментов сохранён ссылками `memory.имя`. Живой прогон — `npm run test:live:tools-memory` (52 проверки на настоящем `main.js` и настоящих файлах: заметка ложится вне проекта, повтор доходит до хранилища, откат возвращает содержимое файла, поиск находит настоящий исходник). Вынесено из `agent-tools.js` в части 40 (заход 6b) |
| `src/agent-tools-mission.js` | инструменты агента — миссии и план: `missionStart` (цель, план и лимиты ложатся файлами в `.agent/missions/<id>/`, прогон переживает перезапуск), `missionStep` (журнал шагов и «дальше»), `missionStatus` (прогресс, план и хвост журнала), `missionFinish` (отчёт файлом; повторное закрытие честно говорит «уже закрыта») и `todoWrite` (план работ панелью-чеклистом и сводка `total/done/failed` для предохранителя «план не закрыт»). Собирается `createMissionTools(deps, live)` — живой мост нужен: роль и чат текущего прогона (миссия помнит, кто её ведёт), лента событий (панель обновляется сразу) и сводка плана пишутся в состояние main.js. Вместе с обработчиками из `agent-tools.js` уехали два помощника замыкания — `notifyMission` и `missionOfRun` (выбирает миссию ПРОГОНА по `live.activeRunMissionId`, иначе агент без id закрывал бы чужую работу); в оболочке они не остались мёртвыми. Тела перенесены байт в байт, порядок инструментов сохранён ссылками `mission.имя`. `todoWrite` переносит план и в ШАГИ миссии прогона (`missionStore.missionSetPlan`: статусы плана → состояния шагов, переходы — строками журнала, «дальше» миссии за планом). Живой прогон — `npm run test:live:tools-mission` (44 проверки на настоящем `main.js`: файлы миссии на диске, сводка плана в состоянии main.js, повторное закрытие). Вынесено из `agent-tools.js` в части 40 (заход 6c) |
| `src/agent-tools-app.js` | инструменты агента — окно приложения и рабочий стол: `appRead`/`appClick`/`appFill`/`appSelect`/`appPress`/`appWait`/`appScreenshot` (работу делает `app-ui-tools.js`, модуль отдаёт ему ЖИВОЕ окно), заглушка `askUser` (инструмент обрабатывает сам прогон — он ждёт ответа человека), `clipboardRead`/`clipboardWrite` (буфер обмена Electron 44 — методы асинхронные), `screenshotDesktop` (источник экрана выбирается по имени окна, файл ложится в папку скриншотов) и `openPath`. Собирается `createAppTools(deps, live)` — мост нужен: окно меняется, а картинка скриншота уходит событием в ленту. Тела перенесены байт в байт. Живой прогон — `npm run test:live:tools-app` (32 проверки на настоящем `main.js`: окно из моста, скриншоты, буфер, открытие пути). Вынесено из `agent-tools.js` в части 40 (заход 8) |
| `src/agent-tools-devtools.js` | инструменты агента — вложения и разработка: докер (`dockerBuild` в папке проекта, `dockerRun` — по умолчанию отсоединённый, `dockerExec` внутри контейнера), установка и проверка программ (`installPackage` — менеджер пакетов проекта (bun/npm/pnpm/yarn) определяется сам, `checkInstalledProgram`, `canExecute`), проверки проекта (`lintProject` — что нашлось: tsconfig/eslint, `runTests` — скрипт из `package.json` или `bun test` по замку, `validateProject` — tsc + eslint + тесты с итогом «сколько этапов успешно», `formatCode` — prettier из `node_modules` проекта, `getDependencies` — объявлено против установленного, с веткой `npm audit`) и разбор (`explainError` — код выхода вместе с командой, `dbQuery` — psql/mysql по строке подключения; клиент получает ВЫДАННОЕ ему окружение `db.query`, а не весь набор переменных агента). Все помощники (15 имён: `agentWorkDir`, `detectPackageManager`, `envFor`, `execFile`, `explainExit`, `findProgram`, `hasLock`, `resolvePath`, `runProgVersion`, `runTerminalCommand`, `stripAnsi`, `summarizeTestOutput`, `truncateText`, `fs`, `path`) приходят тем же плоским `deps`; живого моста не нужно — рабочая папка берётся в момент вызова. Тела перенесены байт в байт, порядок инструментов сохранён ссылками `devtools.имя`. Живой прогон — `npm run test:live:tools-devtools` (36 проверок на настоящем `main.js` и настоящей папке проекта: настоящий `npm test`, локальная установка пакета без сети, честные отказы вместо контейнеров и чужих баз). Вынесено из `agent-tools.js` в части 40 (заход 9) |
| `src/agent-tools-media.js` | инструменты агента — медиа, просмотр и справка: `findTools` (поиск инструмента словами и включение его группы схем через ЖИВОЙ роутер прогона: он один знает, что помещается в окно модели, поэтому модуль берёт его геттером, а не копией), `openUrl` (системным браузером), `showImage`/`previewUI`/`diffView` (человеку уходит СОБЫТИЕ через `live.activeEmit`: картинка data-URL, патч, адрес предпросмотра), `analyzeImage`/`generateImage` (вспомогательная vision-модель; выключена — честный отказ с объяснением, а имя файла из ответа модели путь НЕ задаёт: картинка ложится в рабочую папку), `screenshotCapture` (страница в файл через настоящий `screenshotUrl`), `waitForIdle` (спрос у настоящего `browserTools`) и `agentGuide` (справочники сайтов и тем из данных приложения). Все помощники (11 имён: `fs`, `path`, `agentWorkDir`, `resolvePath`, `truncateText`, `fmtError`, `searchTools`, `shell`, `auxConfig`, `describeImageRemote`, `generateImageRemote`, `unifiedDiff`, `screenshotUrl`, `saveScreenshotPng`, `browserTools`, `agentGuideCall`) приходят тем же плоским `deps`; собирается `createMediaTools(deps, live)`. Тела перенесены байт в байт, порядок сохранён ссылками `media.имя`. Живой прогон — `npm run test:live:tools-media` (45 проверок на настоящем `main.js` с окном-свидетелем: события доезжают, отказы названы, в сеть не ходит). Вынесено из `agent-tools.js` в части 40 (заход 10) |
| `src/agent-tools-vault.js` | инструменты агента — пароли сайтов и почта: `vaultList` (какие сайты сохранены и с каким логином; ПАРОЛЬ в ответ не попадает НИКОГДА — это правило безопасности модуля `src/vault.js`), `vaultFill` (подстановка логина и пароля прямо в форму открытой страницы; заполняет настоящий браузерный набор `browserTools`, пароль в чат не выводится), `mailSend` (письмо от имени пользователя — только если в Настройках → «✉️ Почта» стоит «Разрешить агенту отправлять письма»), `mailList` (последние входящие: отправитель, тема, дата, найденный код, первые строки) и `mailCode` (код подтверждения из свежих писем). Помощники (`vault`, `mail`, `mailConfig`, `browserTools`, `loadSettings`) приходят тем же плоским `deps`; собирается `createVaultTools(deps)`. Настройки читаются В МОМЕНТ ВЫЗОВА — галочка разрешения на отправку действует сразу, без перезапуска. Тела перенесены байт в байт, порядок инструментов сохранён ссылками `vaultTools.имя`. Живой прогон — `npm run test:live:tools-vault` (25 проверок на настоящем `main.js`; в сеть не ходит, почта отвечает честным отказом, браузер не запускается). Вынесено из `agent-tools.js` в части 40 (заход 11) |
| `src/agent-tools-system.js` | инструменты агента — система и установка ПО: `getSystemInfo` (визитка машины: ОС, процессор, память, диски, версии программ), `installSystemPackage`/`installExe`/`wingetSearch` (установка пакетным менеджером или скачанным установщиком: проверка хэша и подписи, предохранитель перед запуском, ветки .exe/.msi/.zip), `refreshEnv` (перечитать PATH после установки), `listProcesses`/`killProcess` (кто жив и как остановить), `registryRead`/`registryWrite` (только разрешённые ветки реестра Windows). Все помощники — из `system-stack.js`, приходят 24 именами в `deps`; живого моста модулю не нужно, собирается РОВНО с одним аргументом. Тела перенесены байт в байт, порядок инструментов сохранён ссылками `system.имя`. Живой прогон — `npm run test:live:tools-system` (41 проверка на настоящем `main.js` и настоящей машине: свой процесс в списке, живое завершение процесса, настоящий PATH, честные отказы вместо установки). Вынесено из `agent-tools.js` в части 40 (заход 5) |
| `src/agent-tools-run.js` | инструменты агента — команды, оболочка и фоновые процессы: `shellsStatus`, `runCommand`, `runCommandOutput` (сбор вывода, маркер готовности, ветка длительного dev-сервера), `runCommandAsAdmin`, `runScript` (скрипт из `package.json`), `startBackground`/`listBackground`/`backgroundOutput`/`sendInput`/`stopBackground`, `shellStart`/`shellSend`, `waitUntil`, `timeoutCommand`, `retryCommand`. Все нужные значения приходят тем же плоским `deps` (22 имени), живого моста модуль не требует и собирается РОВНО с одним аргументом. Тела перенесены байт в байт, порядок инструментов сохранён ссылками. Живой прогон — `npm run test:live:tools-run` (45 проверок на настоящем `main.js`, настоящих процессах и живых PID). Вынесено из `agent-tools.js` в части 40 (заход 4) |
| `src/browser-tools.js` | браузер агента (Playwright): сессия и вкладки (ленивый запуск Edge/Chrome/Chromium, постоянный профиль, подключение к своему Chrome по CDP), действия (open/fill/click/select/press/text), eval/dom/overlays, составное действие (`act`/`normalizeStep`), аудит страницы для деплоя и общие помощники (`sleep`, `waitUntil`, `boxOf`, `ACTION_TIMEOUT`). Карта страницы и поиск элементов вынесены в `src/browser-map.js`, прокрутка/наведение/догрузка — в `src/browser-scroll.js`, работа с формами и replay — в `src/browser-replay.js`, функции страницы — в `src/browser-inpage.js`, сеть и покой — в `src/browser-net.js` (часть 39); модуль их реэкспортирует, поэтому наружу API не менялся |
| `src/browser-scroll.js` | прокрутка, наведение и ленивая догрузка: честное колесо мыши (`wheelAt` — ждёт сдвига событием, а не паузой), состояние прокрутки строкой для агента (`posLine`), «что видно в кадре» с ref (`revealText`), имена элементов для сравнения «что появилось после наведения» (`namesOnPage`), приведение запроса к ref/селектору/имени (`queryFromSpec`), ключ «что изменилось» (`lazyKeyOf` — только содержимое, без позиции прокрутки), догрузка ленивого списка до упора (`loadAllScroll`) и сами инструменты `browserScroll` (`scroll`) и `browserHover` (`hover`). `dom-map.js`, функции страницы из `browser-inpage.js` и карта из `browser-map.js` берутся обычным require, а `needTab`/`sleep`/`waitUntil`/`boxOf`/`ACTION_TIMEOUT` приходят мостом `setBrowserScrollDeps` — состояние сессии модуль за собой не тянет (мост ставится ПОСЛЕ объявления `ACTION_TIMEOUT`, иначе мёртвая зона). Вынесено из `browser-tools.js` в части 39 (заход 5) |
| `src/browser-map.js` | карта страницы и умный поиск цели: классификация слоёв поверх страницы (`overlayKind`/`OVERLAY_LABEL`/`overlayItems` — перевод, cookie, юридическое согласие, диалог), сама карта (`collectMap` поверх `collectInPage`) и поиск элемента (`cssOrTextLocator`, `firstUsable`, `clickCandidates`/`fieldCandidates`, `resolveTarget` — по ВСЕМ фреймам и с адаптивным опросом, `suggestQuery`/`missText` — человеческий текст промаха с подсказками). `dom-map.js` и `collectInPage` берутся обычным require, а общий `sleep` приходит мостом `setBrowserMapDeps` — состояние сессии модуль за собой не тянет. Вынесено из `browser-tools.js` в части 39 (заход 4) |
| `src/browser-inpage.js` | функции, исполняемые в самой странице: их передают в `page.evaluate` по ссылке либо вклеивают через `.toString()`, поэтому они не берут ничего из замыкания модуля (только window/document и аргументы). Карта страницы (`collectInPage`), сериализация значения (`evalValueToPlain`/`unwrapEvalValue`/`varValueInPage`/`describeVar`), уборка помех и согласие (`cleanupInPage`/`acceptTermsInPage`), прокрутка/наведение/покой (`scroll*InPage`, `hoverInPage`, `idle*InPage`), список и его строки в replay (`fetchJsonInPage`/`itemsKeyInPage`/`scrollItemIntoViewInPage`). Вынесено из `browser-tools.js` в части 39 |
| `src/browser-replay.js` | replay и работа с формами: разбор/сборка/маскировка тела формы (`parseForm`/`buildForm`/`maskForm`), пути к элементам и курсору (`pickPath`/`findItemsPath`/`findTotalPath`/`findCursorPath`/`findCursorParam`), ключ строки (`keyOfItem`), выбор образца запроса (`replayFindSample`) и сам `browserReplay` — повторяет запрос страницы её же куками и CORS и листает ответ курсором, крупные данные пишет в файл. Две функции СЕССИИ (`needTab`, `netRecorder`) приходят мостом `setBrowserReplayDeps` — состояние браузера модуль за собой не тянет. Вынесено из `browser-tools.js` в части 39 |
| `src/browser-net.js` | сеть вкладки и покой: кольцевой журнал запросов вкладки (`netRecorder`, `NET_MAX`/`NET_POST_MAX`/`NET_STATIC`), инструмент `browserNetwork` (что страница отправила и что вернул сервер; по умолчанию отдаёт НОВОЕ и очищает буфер) и `browserWaitForIdle` — ждём тишину DOM и пустую сеть. `maskForm` берётся из `browser-replay.js`, `idle*InPage` — из `browser-inpage.js`, а `needTab` и `sleep` приходят мостом `setBrowserNetDeps`. Вынесено из `browser-tools.js` в части 39 |
| `src/browser-ipc.js` | браузер агента со стороны интерфейса: единая точка применения браузерных настроек (`applyBrowserSettings` — папка профиля в userData и режим «свой Chrome» по CDP) и каналы `browser:profileInfo`, `browser:clearProfile`, `browser:connect`, `browser:connectInfo`; настройки читаются в момент вызова мостом `live` (хранилище настроек собирается ниже и само зовёт применение) |
| `src/agent-store.js` | хранилище агента — только СБОРКА (часть 38): наружу отдаются ТЕ ЖЕ имена, что и раньше (main.js, mission-store.js, панель памяти и стенды берут хранилище одним `require`), кода поведения здесь нет — правки делать в модулях ниже |
| `src/store-files.js` | мелочи хранилища: локальный день и время по часам пользователя (`localDayKey`/`localTimeKey`/`localTimeHuman`, `dayKeyValid`), маскировка секретов (`redactSecrets`) и атомарная запись текста (`atomicWriteText`). Состояния нет — путь приходит аргументом. `pad2` здесь ОДИН: в старом `agent-store.js` он был объявлен дважды, и второе объявление затеняло первое (исполнялось именно оно) — при разрезе оставлен исполнявшийся |
| `src/notes-checkpoints.js` | память проекта (`noteSave`/`noteRead`/`noteDelete`; ключ-кириллица переводится в латиницу: «Клиенты ВК» → `klienty-vk`) и точки отката (`checkpointSave`/`checkpointList`/`checkpointRollback`, снимок текстовых файлов рабочей папки) |
| `src/context-memory.js` | память диалогов: сжатые памятки контекста по датам (`contextMemorySave`/`contextMemoryRead`/`contextMemorySearch`/`contextMemoryPrune`/`contextMemoryClear`/`contextMemoryStats`), маскировка секретов перед записью, день и время берутся из `store-files.js` |
| `src/tasks.js` | дела со сроками и повторами (`parseDue` — человеческий разбор «завтра 14:00», повторы `parseRepeat`/`nextDueDate`, доска `tasksBoard`, сводка `tasksSummary`, напоминания `tasksTakeReminders` и авто-запуск `tasksTakeAuto`) |
| `src/yandex-cloud.js`, `yc-*.js` | Yandex Cloud: статус, ресурсы, логи, стоимость, CLI, консоль |
| `src/yc-service.js` | служебный слой Yandex Cloud (`createYcService`): рабочая конфигурация из настроек (`ycConfig`), строка облака для визитки проекта (`ycBriefLine` — «каталог НЕ выбран» дословно, иначе имя/ID каталога, облако и разрешения словами «разрешено»/«ЗАПРЕЩЕНО»; вынесена из оболочки в части 36, визитка берёт её отложенной стрелкой), проверка авторизации, контейнер и ревизии, логи Cloud Logging, встроенный `yc` CLI; модуль ничего не создаёт и не меняет по своей инициативе |
| `src/deploy-*.js`, `cloud-state.js` | деплой: рецепты, состояние, конвейер, проверки |
| `src/mail.js`, `mail-ipc.js` | почта SMTP/IMAP |
| `src/git-ipc.js`, `fs-ipc.js`, `system-stack.js` | git, файлы проекта, стек и окружение |
| `src/mission-ipc.js`, `mission-store.js` | дела, миссии и папка работы агента: каналы `tasks:*`, `mission:*`, `agentfiles:*`. Хранилище миссии держит цель и её **уточнения** (`goalNotes`: новые просьбы человека по ходу работы), план (`steps` — их ставит модель или зеркалит план панели `missionSetPlan`) и **этапы работы** (`stages` — их ведёт приложение: отрезок на батч/остановку с длительностью, файлами, началом и концом; `missionStage`), а также журнал, отчёт и зеркала `tasks.md`/`context` |
| `src/model-ipc.js` | модели, замер локальной модели и G4F: каналы `ai:models`, `ai:probeLocal`, `g4f:probe`, `g4f:test` |
| `src/github-ipc.js` | GitHub: вход по коду, список и поиск репозиториев, выбор и клонирование, публикация нового репозитория: каналы `github:*` |
| `src/settings-store.js` | схема настроек и миграции, чтение и запись (секреты — отдельно), OpenAI-подключения с кулдауном, история чатов: `loadSettings`/`saveSettings`/`loadChats`/`saveChats` |
| `src/bg-processes.js` | фоновые процессы и постоянные оболочки (`bgSpawn`/`bgKill`, кольцевой буфер вывода, остановка вместе с деревом), сбор вывода одной команды (`spawnCollect` — ожидание маркера в выводе, таймаут с убийством ДЕРЕВА, очистка от управляющих последовательностей; сюда переехал из оболочки в части 36), распознавание dev-команд (`SERVER_CMD_RE`), освобождение порта (`killProcessesOnPort` с ПОСТРОЧНЫМ разбором `lsof`/`fuser` — `parsePortOwners`; в PID не попадают номер порта и адрес) и HTTP-проверка адреса (`checkUrlStatus`) |
| `src/rate-limiters.js` | держатели темпа провайдера (`createRateLimiters` → `rateLimiterFor`): один лимитер на пару «провайдер+модель», общий ВСЕМ прогонам — «Стоп» и новый запуск не шлют запросы сразу и не ловят 429 снова (карта в замыкании фабрики, секрет в ключ не попадает). Вынесено из оболочки в части 36; живой прогон проверяет, что темп, узнанный первым прогоном, соблюдён вторым |
| `src/screens.js` | скриншоты: снимок страницы невидимым окном (`screenshotUrl` — скрытое окно 1280×800 по содержимому, 30 с на загрузку и 2,5 с на отрисовку, окно гасится в любом исходе), кодирование (`encodeShot` — JPEG по умолчанию, PNG по `png:true`/`format:"png"`, длинная сторона до 1440, границы 480…2560) и сохранение в `userData/screenshots` (`saveScreenshotPng` — расширение по типу) |
| `src/undo-store.js` | снимки отката правок агента (`snapshotFileForUndo` с вытеснением на файл) и чекпоинт на диске (`persistUndo`/`loadPersistedUndo`, `undoFile`) |
| `src/project-search.js` | обход проекта (`walkProject`), список файлов для «осмотра» (`listProjectFiles`) и поиск по содержимому (`searchProjectFiles`) |
| `src/project-analysis.js` | анализ проекта и чтение файлов инструментами: сбор исходников (`projectSourceFiles`), структура файла (`buildFileStructure`), переименование по границам слова (`refactorRenameFiles`), поиск ссылок на символ (`findSymbolReferences`), нумерация строк (`numberedLines`), язык по расширению (`langFromExt`), карта определений (`buildFileOutline`) и диапазоны блоков (`buildBlockRanges`) |
| `src/project-brief.js` | «визитка» проекта для системного промпта (`createProjectBrief` → `buildProjectBrief`): имя и скрипты package.json, двухуровневая структура без мусора (предел 80 записей и 2 уровня), начало README (10 строк), доступные оболочки и строка Yandex Cloud; настройки читаются в момент сбора — визитка собирается на каждый прогон |
| `src/shell-tools.js` | оболочки и запуск команд: диалект (cmd/powershell/pwsh/bash/sh) с псевдонимами по-русски, кодировка вывода (chcp 65001, UTF-8 в PowerShell) и `-EncodedCommand`, поиск Git-оболочки с человеческой подсказкой при её отсутствии, отчёт о доступных оболочках и запуск команды без интерактива с кодом возврата и временем; команда запускается через `spawn` СО СВОЕЙ ГРУППОЙ процессов (`detached`), поэтому по таймауту гасится всё ДЕРЕВО, а не одна оболочка, а предел накопленного вывода (32 МБ) держит сам модуль; вывод режется по каждому потоку отдельно (`stdout` и `stderr` — по 3000 знаков, предел 6000) с честной пометкой, сколько было всего (`normalizeShell`/`resolveShell`/`shellsBrief`/`shellsStatus`/`runTerminalCommand`) |
| `src/tasks-reminders.js` | дела между списком и человеком: зеркало `.agent/tasks.md` в рабочей папке (галочка «файлы работы»), напоминания по сроку (системное уведомление только при живом канале — в контейнере без D-Bus фон выживает, а тост всё равно уходит окну), отметка «напомнено» в хранилище (пристаём один раз), автозадача уходит прогоном с меткой `from: "desktop"`, дела без окна НЕ берутся и ждут его возвращения, точный будильник на ближайший срок (`Math.min(ms + 250, 6 ч)`); окно читается в момент вызова (`userDataDir`/`emitTasksChanged`/`canNotify`/`notifyUser`/`armTaskWake`/`checkTaskReminders`) |
| `src/tool-helpers.js` | помощники инструментов: пакетный менеджер по lockfile (`detectPackageManager`/`hasLock`), сводка итога тестов (`summarizeTestOutput`), unified-дифф через git (`unifiedDiff`) |
| `src/app-window.js` | главное окно: параметры и preload, прокси событий в мобильный мост, метка запуска у `ai:event`, ссылки в браузере пользователя, напоминания о делах, обнуление окна при закрытии (`createWindow`) |
| `src/terminal-panel.js` | пользовательский терминал (нижняя панель): постоянная оболочка рабочей папки, дублирование команд и вывода агента, автодополнение (`termStart`/`termInput`/`termStop`/`termStatus`/`termShutdown`/`termComplete`) и СВОИ каналы `term:start`/`term:input`/`term:stop`/`term:status`/`term:complete` (`registerTermIpc`); старт читает рабочую папку в момент вызова — проект мог переключиться, пока панель была закрыта |
| `src/run-mission.js` | миссия прогона агента (долгая работа): авто-миссия на шестом раунде, журнал по действиям, защита от цикла и стояния на месте, граница батча (25 раундов), **закрытие этапа работы** на границе батча, при остановке по лимиту, паузе и «Стоп» (в нём остаётся, что делали и на чём встали), **уточнение цели новой просьбой человека** на продолжении, сводка состояния работы для модели (`digestMessage` — из файлов миссии, читаются с диска каждый раз: цель и её уточнения, план, путь по этапам, журнал, что переживёт следующую команду; с пределом размера и сокращением, когда не влезла), призывы сторожа и пауза с сохранением работы (`createRunMission`, состояние — один объект `state`) |
| `src/run-tools.js` | роутер инструментов прогона: какие схемы уходят в запрос (база + липкие группы), вес того, что реально уйдёт, бюджет истории (резерв 15% и минимум 1500), предохранитель A (вызов вне набора дотягивает группу), предупреждение о тесном окне и автоподключение справочников групп (`createRunTools`, мост `router` для `findTools`) |
| `src/run-retry.js` | восстановление прогона после отказа запроса: ожидание лимита 429 (до бюджета ожидания) и повтор ТОГО ЖЕ раунда, повтор «холодного» отказа пула (503 cache_only_cold), отключение stream_options на строгом сервере, ужатие истории при переполнении контекста, русские объяснения 402 и прочих отказов, темп провайдера заранее; коды ШЛЮЗА (520–527, 504, 530) и обрыв чтения тела запроса (`BODY_READ_RE`) повторяются тем же правилом, а оборванная СВЯЗЬ уходит в `transport` со своими паузами 2/5/10 с (постоянные сетевые ошибки — наружу сразу) (`createRunRetry`, состояние — один объект `state`) |
| `src/run-round.js` | один раунд прогона: сборка запроса (схемы роутера, справочники сразу за системным промптом, бюджет и окно модели — живыми значениями), чтение потока ответа с отделением рассуждений и пометкой обрыва, метрики раунда в «Консоль» и цена работы в миссии (`createRunRound`, возврат `repeat`/`error`/`ok`; повтор раунда и падение — решение прогона; оборванную связь (`fetch failed`, ECONNRESET) раунд ОТДАЁТ решению модуля повторов `retry.transport`) |
| `src/run-calls.js` | вызовы раунда: разбор JSON-вызова, напечатанного моделью в тексте (до призывов «план не закрыт»), нормализация имён, стабильные id, дедупликация одинаковых вызовов, запись assistant с `tool_calls` и подписью мысли (Gemini `extra_content`), параллельная пачка read-only инструментов (`createRunCalls`; решение о батчинге — `canRunParallel`, выполнение — `runParallel`, хозяином цикла остаётся прогон) |
| `src/run-strict.js` | строгая очередь вызовов раунда (когда пачкой нельзя): подтверждение опасной команды (`toolPolicy.isDangerousCommand`) и инструмента с высоким риском (`toolPolicy.needsConfirm`), вопрос человеку `askUser`, чекпоинт отката перед правкой файла (`snapshotFileForUndo`), журнал действий с источником `desktop`/`mobile` (`audit.record`) и журнал миссии (`mission.noteCall`), запрет инструментов в режиме плана (`createRunStrict`, `runStrict(calls, { planMode, history })`; остановка по «Стоп» — за прогоном) |
| `src/run-batch.js` | решения после раунда: разовая просьба об итоговом отчёте при пустом ответе (`askForReport`), граница батча — продолжение тем же контекстом с напоминанием человеку и в историю, мягкий финал закрытой миссии «🏁 Работа закончена», выход из цикла батчей (`afterRound`, возврат `end`/`break`/`continue`; завершение и выход — решение прогона) |
| `src/run-ai.js` | прогон агента целиком: сборка запроса (роль, рабочая папка и активный репозиторий, визитка проекта, сводка дел менеджеру, **сводка состояния работы в КАЖДЫЙ запрос — вторым сообщением и снимается сразу после раунда, чтобы не копилась в сохранённой истории**, толчок после клона, одноразовые примечания), бюджет и окно модели, цикл «батчи → 25 раундов» с повтором раунда без траты номера и раунда миссии, решение о пачке/строгой очереди, пауза и «Стоп» с журналом отката, финал с объяснением пустого ответа, авто-чекпоинт и уведомление только при неактивном окне, авто-повтор после сбоя (до двух) с примечанием в историю и переключением OpenAI-подключения, снятие `stream_options`, ужатие контекста (40% бюджета), закрытие этапа работы при «Стоп» (`mission.stage`) и мягкое завершение задачи (`createRunAi`; живое состояние оболочки — мостом `live` с геттерами и сеттерами) |
| `src/run-nudge.js` | призывы по текстовому ответу: «план не закрыт» — просьба продолжить делом (не больше двух раз за прогон, текст про `failed` для невыполнимых пунктов), сторож миссии (`mission.canNudge`/`nudge`) с призывом или паузой (`createRunNudge`, возврат `repeat`/`pause`/`none`; повтор раунда и финал — решение прогона) |
| `src/site-guides.js` | справочники агента по сайтам и темам: встроенные (`src/agent-guides/*.md`) и выученные в папке приложения, подхват по адресу (`guideForUrl`) и действия инструмента `agentGuide` (`list`/`match`/`read`/`save`) |
| `src/agent-env.js` | окружение агента и назначения инструментов: переменные человека (`settings.agentEnv`) вместе с автоматическими YC_* и свежим IAM (обмен OAuth→IAM и продление за 5 минут до истечения), ключи путей только для «слепых» команд (`pathOnlyEnv`), выдача переменной по её назначению (`envFor`/`commandEnv`), пробы без секретов (`probeEnv`), PATH встроенного yc CLI (`ycEnsurePath`), пересборка окружения (`applyAgentEnv`/`rebuildAgentEnv`) и живое состояние — выполнение под назначением (`withCapability`, `getCapability`/`setCapability`) |
| `src/paths-git.js` | пути и git для всего бэкенда: `resolvePath`/`sanitizeDir`/`sanitizePath`, `ensureWritableDir` (можно ли писать в папку: создание папки, проверка прав и НАСТОЯЩАЯ проба записи с уборкой за собой — git падает даже там, где `accessSync` прошёл: OneDrive, сеть, защищённые папки; перенесена из оболочки в части 36), `gitDirOrHome`/`agentWorkDir`, `repoNameFromUrl`/`stripUrlCreds`, `runGit` (окружение по назначению + Basic-авторизация) |
| `src/git-stage.js` | гит-операции агента: индексация без файлов секретов (`stageAllSafe` — исключение `.env*` в самой команде плюс страховка, снимающая с индекса проскочившие имена вида `*.env`) и авто-чекпоинт после задания (`autoCheckpointCommit` — один локальный коммит с заголовком из последнего вопроса человека, никогда не пушит и молчит, когда коммитить нечего) |
| `src/chats-ipc.js` | история чатов со стороны интерфейса: три канала (`chats:load`, `chats:save` и синхронный `chats:saveSync` при закрытии окна) и сигнал `chats:reload` остальным клиентам — тому, кто сохранил, сигнал не идёт, а клиент телефона приходит с фиктивным отправителем (id = 0) и сигнал получает; окно берётся живым через `getWindow` |
| `src/memory-ipc.js` | память диалогов со стороны интерфейса: четыре канала панели (`memory:stats` со сроком хранения и выключателем, `memory:days` со списком дней и каталогом, `memory:openDir` с показом папки в системе, `memory:clear` по дате или целиком с понятным итогом) — путь и содержимое дневника считает бэкенд, а настройки читаются в момент вызова; отсюда же запись памятки при сжатии контекста (`saveContextMemo`: зеркало `.agent/context` рядом с проектом по галочке «файлы работы агента» и дневник памяток по галочке «Память диалогов» — два условия НЕЗАВИСИМЫ; перенесена из оболочки в части 36) |
| `src/settings-ipc.js` | настройки со стороны интерфейса: каналы `settings:get`, `settings:set`, `policy:groups` и `dialog:pickDir` (выбор рабочей папки в системном диалоге: его зовут и настройки, и панель проекта, родителем идёт живое окно, а на закрытом окне — без родителя, иначе Electron бросил бы ошибку) (группы выдачи — из таблицы прав `tool-policy.js`); `settings:set` защищает от обрезанного или устаревшего объекта интерфейса — пароли сайтов, пароль почты и выбор каталога Yandex Cloud не стираются сохранением без них (пустой список паролей — осознанная очистка), — применяет настройки к окружению агента, профилю браузера и мобильному мосту, а при включённом мобильном доступе без PIN генерирует его; смена рабочей папки сбрасывает «активный репозиторий» сеттером моста `live` (переменная принадлежит оболочке — её читают пути-и-git, GitHub-каналы и инструменты) |
| `src/mobile-ipc.js` | мобильный доступ со стороны интерфейса: `mobile:status` отдаёт статус живого моста, `mobile:pinRegen` ставит новый шестизначный PIN, сохраняет его и применяет к живому мосту (иначе окно показало бы новый PIN, а телефон остался бы подключён по старому) |
| `src/projects-ipc.js` | проекты (до 10) со стороны интерфейса: каналы `projects:list` (свежие сверху, отсутствующая папка помечена), `projects:create` (имя и папка, лимит 10, занятая папка, проверка прав на запись), `projects:activate` и `projects:remove` (папка на диске не удаляется); смена активного проекта — и при переключении, и при удалении активного — сбрасывает три живых значения оболочки сеттерами моста `live`: «активный репозиторий» (иначе агент остался бы в папке прошлого проекта), флаг «только что склонирован» и снимки отката |
| `src/preview-ipc.js` | быстрый запуск проекта (превью): каналы `dev:start`/`dev:stop`/`dev:status` и вывод сервера событиями `dev:event`; автоопределение команды по package.json (dev/start/serve, запуск через bun), отказ от второго сервера, остановка с освобождением порта из настроек (процесс мог запустить агент или он остался от прошлого запуска), состояние запуска и остановка при выходе приложения (`devShutdown`) живут в модуле, окно приходит функцией |
| `src/ota-ipc.js` | самообновление со стороны интерфейса: каналы `ota:status`/`ota:check`/`ota:rollback`/`ota:openDir`/`ota:reset` (падение применения превращается в `{status:"error"}` — иначе окно показало бы «обновлений нет» на упавшей проверке; флаг удаления исходников приводится к булеву; настройки читаются в момент вызова — в них выключатель self-update) и подписка штатного апдейтера сборки (`initAutoUpdater`: заголовок окна и уведомления, проверка раз в час, окно берётся живым) |
| `src/run-ipc.js` | прогон агента и откат правок со стороны интерфейса: каналы `ai:send` (запуск `runAi` с отметками прогона — источник, роль, чат и сброшенная миссия; сбой возвращается окну значением `{ok:false}` и уносит сделанные до него правки в журнал отката), `ai:answer` (ответ человека на `askUser`), `ai:stop` (флаг остановки, прерывание запроса и снятие ожидания ответа), `ai:test` (проверка подключения по текущим настройкам с наложенными полями окна) и `undo:status`/`undo:rollback` (чекпоинт последнего запуска: файлы возвращаются на прежнее содержимое, созданные агентом — удаляются, сбой на одном файле не срывает остальные и виден в отчёте); живое состояние оболочки (отметки прогона, снимки и журнал отката, ожидание ответа, прерывание и окно) приходит мостом `live` с обеими сторонами — копия «застыла» бы, и прогон с телефона пометился бы как «с ПК» |
| `src/tool-registry.js` | реестр агентских инструментов и вызов инструмента: сборка обработчиков (`createAgentTools` получает ~120 имён тем же плоским списком, что и раньше) и сам `executeTool` — выбор обработчика, объявление назначения (`toolPolicy.capabilityOf`) на время вызова с возвратом в `finally`, немедленная остановка по «Стоп» ДО вызова и единая обёртка ошибки вместо исключения; рядом — `describeToolArgs` (обёртка над `toolPolicy.describe`, ей пользуются прогон и строгая очередь при подтверждении); живое — только назначение окружения (экземпляр `agent-env` приходит из оболочки: своя копия дала бы пустое назначение) |
| `src/app-ui-tools.js`, `tool-policy.js` | интерфейсные инструменты, разрешения |
| `src/mobile-bridge.js`, `secrets.js`, `ota.js`, `vault.js` | телефон, секреты, обновления, пароли |
