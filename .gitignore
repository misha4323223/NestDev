node_modules/
dist/
web/
build/
*.log
.idea/
*.iml

# Локальный OTA-бандл (генерируется scripts/make-ota.js)
ota/*

# Копия удалённого playwright (только локально, в репозиторий не попадает)
backup-playwright/
# Но рабочий бандл для доставки обновлений — коммитим (версия выше сломанной)
!ota/manifest.json
!ota/bundle.json
