# Передача работы: этап B (вынос логики из `main.js` в модули)

Обновлено: 21 сентября 2026, после части 37 (жизненный цикл вынесен — этап B закрыт) и правки счётчика «Просрочено» в панели дел. Последний коммит в `main`: `8126719`; части 36 и 37 лежат в рабочем дереве неотправленными.
Этот файл — точка входа для продолжения работы. Читать вместе с хвостом `AGENT-NOTES.md` и `ARCHITECTURE.md`.

---

## 1. Что за проект

Electron-приложение «локальный ИИ-агент»: чат с агентом (локальные модели Ollama и облачные), который работает с файлами, терминалом, git/GitHub, браузером (Playwright), деплоем, Yandex Cloud; есть мобильный мост по локальной сети (PWA + PIN), OTA-самообновление, почта, хранилище паролей.

- `src/main.js` — главный процесс (сейчас 1235 строк, из них почти всё — импорты и блоки `createX({...})`; живых функций с поведением не осталось);
- `src/renderer/agent-core.js` — ядро моделей, общее для окна и мобильного клиента;
- `src/agent-tools.js` — инструменты агента (154 обработчика);
- `src/renderer/*` — интерфейс.

**Этап B** — структурная работа: из главного процесса выносятся куски логики в свои модули. Все 121 канал `ipcMain.handle` регистрируют модули (в `main.js` каналов нет — остался только прокси `ipcMain.handle` для мобильного моста). **Все части, включая жизненный цикл (37), сделаны** — в оболочке остались только импорты, блоки `createX({...})` и `startLifecycle()`.

## 2. Состояние на момент передачи (проверяемо)

| Что | Значение |
|---|---|
| `src/main.js` | 1235 строк: только импорты, сборка модулей и мосты `live`; живых функций с поведением нет (`fetchModels` оставлен намеренно как шов) |
| Каналы IPC | 121, все в модулях; `main.js` держит только `ipcHandlerMap` для телефона |
| `npm test` | 64 набора, 1333 проверки, 0 падений (exit 0) |
| Живые прогоны | 30 скриптов `scripts/live-*.js` + `npm run test:boot` (настоящий Electron); все зелёные, `test:live:desktop` — 124 ✅ / 0 ❌ (падения в фильтрах дел исправлены, см. §6). `test:live:yc:real` — нужны ключи владельца |
| OTA | 1.5.191, `ota/bundle.json` + `ota/manifest.json`, sha256 сходится с диском |

## 3. Железные правила (почему каждая часть идёт долго)

Ритуал на каждую часть, ни один шаг не пропускается:

1. **Перенос байт в байт** — комментарии и тексты ошибок не переписываются; отличаться может только разбор живого состояния.
2. **Аудит переноса**: `node scripts/audit-extractions.js HEAD WORKTREE <модули>` — делит унесённые строки на «как есть», «переименование» и «требует глаз»; каждая строка из третьей группы должна быть объяснена.
3. **Набор тестов** `test/<модуль>.test.js`, и он обязан быть в цепочке `npm test` (набор не в цепочке = набора нет).
4. **Живой прогон** `scripts/live-<часть>.js` на **настоящем `main.js`** (перехват `require`), с настоящими файлами на диске и настоящими каналами/функциями.
5. **Негативные контроли**: сломать поведение → убедиться, что покраснело (набор и/или живой прогон) → вернуть файлы и сверить sha256. Контроль, который ловит только набор, — это нормально, но так и надо написать.
6. **Сторожа переноса**: `test/smoke.test.js` (`backendSrc`, два `scanWiring`), `test/repo-shape.test.js` — их надо учить новому модулю.
7. **Записи**: `ARCHITECTURE.md` (карта) + `AGENT-NOTES.md` (что и почему).
8. **Бамп версии** в `package.json` + `node scripts/make-ota.js --version X.Y.Z`, затем сверка sha256 манифеста с диском.
9. **Прогон всего**: `npm test`, живые прогоны, `npm run test:boot`.

Два принципа, которые дали почти все находки:

- **Живое состояние — только мостом, не копией.** Если значение пишут несколько мест, оно идёт через `live: { get, set }` в `main.js`; копия «застывает», и агент уходит в чужой проект, чужой чат или не находит свои снимки отката.
- **Часть обязана искать неправильное поведение, а не только переносить.** Так нашлись: порт «убивал» чужие PID (часть 31), удаление активного проекта оставляло «активный репозиторий» удалённого проекта (часть 31), `gitPublish` звал несуществующую функцию и публикация на GitHub не работала вовсе (часть 35).

## 4. Ловушки, найденные практикой

1. **Страж связи слеп к именам, которых нет в `main.js`.** `scanWiring` ищет имена, которые ЕСТЬ в оболочке, но не переданы модулю. Если имя не передано нигде, искать нечего — именно так прожила незамеченной дыра `publishLocalToGithub`. Лечится обратной проверкой: каждое имя из распаковки депса обязано иметь значение в собранном объекте (в наборе — по тексту, в живом прогоне — по факту). Трогаешь депсы модуля — добавляй обратную проверку.
2. **Правка тестов скриптом** легко задевает чужую строку (был случай: объявление `const main` появилось дважды). Границы правки задавать по маркеру, а не по номеру, и не вставлять строку, содержащую маркер следующей замены. Испортил — `git restore --source=HEAD -- <файл>` и заново.
3. **Проверка должна ловить смысл, а не ожидание.** Первая версия проверки «новый прогон начинается с чистого листа» ждала флаг остановки после прогона (а он снимается в `finally`) — упала, и это была ошибка проверки, а не кода.
4. **Поддельные окна живых прогонов** обязаны иметь всё, что имеет настоящее (`setWindowOpenHandler` и т.п.), иначе `createWindow()` падает на середине, цепочка `whenReady` обрывается, и прогон слепнет (так не была видна подписка апдейтера).

## 5. Часть 36 — последние 9 функций с поведением (~193 строки)

| Функция | Строки в `main.js` | Размер | Куда | Зачем / на что смотреть |
|---|---|---|---|---|
| ✅ **сделано в заходе 3** — `rateLimiterFor` + карта | 5–11 | 7 | `src/rate-limiters.js` (новый модуль) | Лимитер темпа; сейчас карта `Map` и функция лежат в оболочке и передаются в `run-ai`. Смысл проверки: лимитер **один** на «провайдер+модель» и общий для всех прогонов (иначе «Стоп» + новый прогон рвут лимит и провайдер отвечает 429). Перед переносом проверить, не подставляют ли её через депс `test/run-ai.test.js` и живые прогоны. |
| ✅ **сделано в заходе 3** — `ycBriefLine` | 298–312 | 15 | `yc-service.js` | Строка Yandex Cloud в визитке проекта (каталог, облако, разрешено ли создание/удаление). `ycConfig` уже живёт в `yc-service.js`, поэтому передача значением отпадёт; ветка «каталог НЕ выбран» должна остаться дословно. |
| ✅ **сделано в заходе 2** — `spawnCollect` | 351–386 | 36 | `bg-processes.js` | «Запустить команду и собрать вывод» с таймаутом, ожиданием строки и убийством **дерева** процессов. Именно у `bg-processes` уже есть все зависимости (`spawn`, `commandEnv`, `stripAnsi`, `killProcessTree`) — новых проводов не потребуется. Альтернатива `shell-tools.js` тянет `spawn` + `killProcessTree` и риск циклического require (`bg-processes` получает `runTerminalCommand` из `shell-tools`). Потребители — три места в `agent-tools`. |
| ✅ **сделано в заходе 1** — `screenshotUrl` + `encodeShot` + `saveScreenshotPng` | 373–449 | 85 | новый `src/screens.js` | Снимок страницы невидимым окном, сжатие (JPEG по умолчанию, PNG по флагу, длинная сторона до 1440) и сохранение в `userData/screenshots`, чтобы агент прочитал файл через `analyzeImage`. Это единственное место, которое создаёт своё окно, — держать рядом с окнами правильнее, чем в оболочке. Проверять: несработавший `did-finish-load`, таймаут 30 с, `png:true`, ужатие и `maxWidth` вне границ. |
| ✅ **сделано в заходе 3** — `saveContextMemo` | 641–669 | 29 | `memory-ipc.js` | Памятка диалога: сначала зеркало `.agent/context/<дата>.md` (по условию `agentWorkFiles`), потом дневник памяти (по галочке `contextMemory`) + сообщение в чат. **Два разных условия в одной функции** — главный риск: их легко слить в одно. Проверка обязана держать оба случая по отдельности. |
| ✅ **сделано в заходе 3** — `ensureWritableDir` | 1014–1052 | 39 | `paths-git.js` | «Можно ли писать в папку»: `mkdir` + `accessSync(W_OK)` + **реальная проба** создания и удаления подпапки (git падает даже когда `accessSync` проходит — OneDrive, сетевые и системные папки). Живёт рядом с `sanitizeDir`/`resolvePath`. Потребители — `github-ipc` (5 мест), `projects-ipc`, `git-ipc`: им передаётся значение, после переноса будут брать из `paths-git`. Тексты подсказок менять нельзя. |
| `fetchModels` | 622–624 | 3 | **оставить в `main.js`** | Чистый переход в `listModels` ядра, и существует ровно как шов для живых прогонов (они его подставляют). Перенос обесценит шов. Так и записать, чтобы к нему не возвращались. |

Почему часть 36 важна: у этих функций **нет своих наборов** — они живут в оболочке и проверяются косвенно. Перенос = своя проверка + живой прогон, то есть впервые появляется шанс поймать то, что сейчас не видно.

Что это уже дало (заход 1, скриншоты): живой прогон нашёл **снимок не того размера** — страница снималась 1280×773, а инструмент обещал человеку «снят (1280×800)» (в Electron `width`/`height` — размер окна, рамка съедала 27 точек). Исправлено `useContentSize: true`, проверено живым прогоном. Появились `test/screens.test.js` (11) и `test:live:screens` (19), в цепочке `npm test` и в списке живых прогонов.

Заход 2 (сбор вывода): `spawnCollect` переехал в `bg-processes.js` без новых проводов; в наборе модуля +4 проверки (настоящие процессы: маркер, таймаут с убийством дерева, отказ запуска), а `test:live:shell` вырос до 8 раундов (настоящие инструменты `runCommandOutput` / `timeoutCommand` / `retryCommand` через живой `main.js`). Попутно всплыл хрупкий страж в smoke: он требовал `shellArgsFor` именно в `main.js`, где это был единственный вызов из ушедшего куска, — теперь проверка смотрит на того, кто запускает команду.

Заход 3 (остальные четыре куска): `rateLimiterFor` → новый `src/rate-limiters.js` (у темпа нет другого дома — он про провайдера, а не про прогон), `ycBriefLine` → `yc-service.js`,
`ensureWritableDir` → `paths-git.js`, `saveContextMemo` → `memory-ipc.js`. `main.js`: 1324 → **1241**.
Две проводки — отложенными стрелками (`saveContextMemo` в прогон, `ycBriefLine` в визитку): их модули
собираются НИЖЕ потребителей, и значение в момент сборки было бы `undefined`.

Заглушки заменены настоящим кодом там, где они стояли ВМЕСТО функции: визитка проекта
(`test/project-brief.test.js`) и её живой прогон (`scripts/live-project-brief.js`) теперь берут
НАСТОЯЩУЮ строку из `yc-service.js` (живой раздел [5] проверяет три её состояния). В
`test/run-ai.test.js` заглушка `saveContextMemo` оставлена осознанно: это граница модуля прогона,
а настоящая запись памятки проверяется своим набором (`test/memory-ipc.test.js`) и живым прогоном
(`test:live:retry`, раздел [12] — до него можно дойти только через сжатие контекста, см. заметки).
`fetchModels` решено оставить в `main.js` именно как шов для живых прогонов.

**Две находки захода 3 (подробно — в `AGENT-NOTES.md`).** Первая: негативный контроль на темп
(`rateMemory`) старый живой прогон НЕ ловил — пауза `Retry-After` и пауза прогона совпадают, и
разницы внутри одного прогона не видно; добавлен раздел [11], где темп, узнанный первым прогоном,
обязан соблюсти ВТОРОЙ (живой разрыв ≈10000 мс против 6 мс со сломанной картой). Вторая: до
`saveContextMemo` в живом прогоне нельзя дойти без длинной истории (сжатие контекста наступает
только когда голова больше 4000 токенов И больше бюджета) и без второго адреса провайдера — окно
модели кешируется ПО АДРЕСУ; заодно выяснилось, что историю чата ведёт интерфейс и присылает её
ЦЕЛИКОМ — стенд обязан копить её сам.

## 6. Часть 37 — жизненный цикл: ✅ сделано

**Готово:** `src/lifecycle.js` (`createLifecycle` → `startLifecycle`, `runShutdown`, `shutdownOrder`, `failures`). `main.js`: 1241 → **1235**. Старт (`whenReady`: окно, настройки моста, подписка апдейтера, тик OTA через 5 с и далее каждые 60 с, `activate`) и остановка (браузер → фоновые процессы → терминал → превью → мост) собираются шагами `SHUTDOWN_STEPS`.

**Находка (главная).** Шаги шли подряд, и отказ первого обрывал всю цепочку: мост телефона с негодным портом в настройках (99999) роняет `listen` СИНХРОННО — и апдейтер не подписывался, а тик OTA не ставился, то есть приложение молча перестаёт видеть обновления сборки. Теперь каждый шаг идёт через `runStep`: отказ виден в логе и записывается, но НЕ отменяет остальные. Живой прогон: до правки 0 подписок апдейтера, после — 6.

**Второе.** Порядок остановки никем не сторожился: упавший шаг оставлял dev-сервер держать порт (следующий запуск — «порт занят»). Теперь порядок проверяется набором и живьём — по настоящим процессам и портам, а не по «функция вызвана».

**Проверки:** `test/lifecycle.test.js` (**11**) + `npm run test:live:lifecycle` (**29** проверок на настоящем `main.js`: https-мост, подписка апдейтера, тик OTA, настоящие фоновый сервер / оболочка / dev-сервер живы, затем мертвы и порты свободны; ребёнком — сломанный мост и упавший шаг остановки). Аудит переноса — 26 строк ушло, 13 как есть, 13 объяснены. Контроли: снять изоляцию шагов, поменять порядок и убрать очистку карты, убрать `.catch()` у браузера, убрать повтор тика — все ловятся (кроме «отклонённого обещания браузера» — только набором: настоящий Playwright в песочнице не отказывает). Разбор — «7.3» в хвосте `AGENT-NOTES.md`.

**Сторожа, которым пришлось показать модуль:** `bg-processes`, `ota-ipc`, `preview-ipc`, `terminal-panel`, `app-window`, `settings-store` (счётчик `loadSettings()`), `smoke.test.js` (`backendSrc` + два `scanWiring`). Правка `smoke.test.js` точечным инструментом НЕ берётся — только патч-скриптом с якорем.

**Найдено попутно — и уже исправлено (счётчик «Просрочено»).** `test:live:desktop` давал 3 падения в фильтрах дел: просроченное показывалось как «сегодня», счётчик «Просрочено» нулевой, активный фильтр не подсвечен. Причина оказалась не в панели и не в подсчёте, а в разборе срока: `parseDue` (`src/agent-store.js`) искал время шаблоном без секунд и таймзоны, поэтому полная метка (`2026-09-21T15:18:19.656Z` — так её отдают `Date.toISOString()`, мобильный мост, правка срока в панели) теряла время и превращалась в «сегодня 09:00», `allDay: true`. Из-за этого падение выглядело плавающим: до 09:00 дело честно лежало в «Сегодня», после — в «Просрочено». Теперь метка читается целиком (дата и время — из одного разбора, смещение учитывается), «2026-09-15» без времени по-прежнему «весь день» в 09:00, негодный час («25:00») разбор не роняет. Проверки: `tasks` в `test/smoke.test.js` (+ отдельный набор «срок из полной метки времени»), `test:live:desktop` — **124 ✅ / 0 ❌**, контроль (отключить чтение времени) ловится и набором (2), и живьём (1). Разбор — в хвосте `AGENT-NOTES.md`.

## 7. Не входит в эту цепочку

- `smoke.test.js` — по твоему решению оставлен на самый конец; в этом плане его нет.
- ~~**Безопасность IPC**~~ — **сделано**, все пять пунктов: (1) `settings:get` больше не отдаёт значения секретов телефону (`src/secret-mask.js`, маска разворачивается при сохранении); (2) у разрушительных каналов появилась проверка отправителя (`src/ipc-guard.js`: окно по живому `webContents`, телефон по `sender.id = 0`, чужому рендереру отказ); (3) у моста телефона теперь список РАЗРЕШЁННОГО вместо одного запрета (`ALLOW` в `src/mobile-bridge.js`); (4) PIN ушёл из QR-кода — вместо него одноразовый токен пары и сеанс устройства; (5) `ai:test`/`ai:models`/`ai:probeLocal`/`g4f:*` проверяют адрес перед запросом (`src/net-guard.js`: link-local и облачные метаданные в числовых и IPv6-формах запрещены, `localhost` и LAN разрешены). Разбор, находки и честные оговорки — в хвосте `AGENT-NOTES.md` («Этап 4»).
- ~~**TLS у мобильного моста**~~ — **сделано** («Этап 5» в хвосте `AGENT-NOTES.md`): мост поднимается по https с самоподписанным сертификатом (`src/bridge-tls.js`, чистый Node: DER собирается вручную, SAN с адресами ПК), ссылка в QR и сокет телефона — `https`/`wss`. Сертификат лежит в `userData/bridge-tls`, переиспользуется и пересоздаётся при смене адресов. Проверки: `test/bridge-tls.test.js`; живой прогон моста идёт по `wss`.
- ~~**OTA-набор без подписи**~~ — **сделано**: подпись Ed25519 (`src/ota-sign.js`, ключи — `src/ota-trust.js`), проверка идёт ДО распаковки в `src/ota.js`; сборка набора подписывает (`scripts/make-ota.js`), ключ заводится `npm run ota:key` (закрытый у владельца, в репозиторий не едет).
  **Осталось за владельцем (одна команда):** `npm run ota:key -- --trust` на своём ПК — пока список доверенных ключей пуст, наборы принимаются без подписи, и об этом предупреждает панель «Обновление кода». После этого сборка набора требует ключ (`--unsigned` — только осознанно).

## 7.1. Сводка состояния работы — сделано (после этапа 5)

Хозяин приложения сообщил: агент после первого батча «забывает», что уже начал делать, и ищет
контекст заново. Причины оказались в приложении, а не в модели: история обрезается с конца, а
журнал/план/прогресс миссии лежат файлами и в запрос не попадали. Теперь `mission.digestMessage()`
собирает сводку из файлов, а прогон подставляет её вторым сообщением в каждый запрос и снимает после
раунда (`finally`). Живые мосты — `get agentEnv` и `get backgrounds` в `main.js`.

**Ловушки этого места (стоят времени):**

- на проводе сводка — ХВОСТ единого системного блока: `mergeLeadingSystem` в `provider-transport.js`
  склеивает ведущие системные сообщения. Проверять «отдельное сообщение» бессмысленно;
- стенды наборов, которые хранят ССЫЛКУ на `messages` раунда, после `finally` увидят пустоту — нужен
  снимок (`slice()`);
- у поддельной миссии в `test/run-ai.test.js` не было `digestMessage`, и набор падал целиком.

Проверки: `test/run-mission.test.js` (35), `test/run-ai.test.js` (25), `test:live:run` (разделы [9],
[10], [11]); негативные контроли `--break=nodigest`, `--break=digeststays`, `--break=nostage`,
`--break=noamend` — все ловятся.

### 7.2. Этапы работы миссии — сделано (сразу после 7.1)

Продолжение той же темы: миссия стала помнить ПУТЬ, а не только последний хвост. Хозяин описал так:
работа началась с «сделай всё сам», после батча агент видел только эту фразу и не знал, что делал.

- отрезок работы (этап) закрывает ПРИЛОЖЕНИЕ: граница батча, остановка по лимиту/времени/циклу,
  пауза и «Стоп» → `missionStore.missionStage`; в записи длительность, раунды, файлы отрезка,
  его начало и конец, «далее» или причина остановки;
- сводка показывает путь: раздел «Этапы работы» (начало + свежие), «с чего начали», «чем закончился
  прошлый отрезок», уточнения к цели; у сводки появился предел размера с сокращением;
- новая просьба человека по ходу миссии стала «уточнением к цели» (`missionGoalNote`), а служебный
  текст кнопки «▶ Продолжить» — нет (`isResumeText` рядом с самим текстом);
- **важная находка:** сводка читала КОПИЮ миссии из памяти, обновляемую только на границе батча, —
  поэтому отметки шагов и этапы отставали до 25 раундов. Теперь она читает миссию с диска по id
  (`fresh()`), а по закрытой миссии не подставляется.

Проверки: `test/run-mission.test.js` (35), `test/run-ai.test.js` (25), `test:live:run` ([10] и [11]);
контроли `--break=nostage` и `--break=noamend`. Разбор — в хвосте `AGENT-NOTES.md`.

**Ловушка места:** в стендах наборов массив сообщений раунда и поддельная миссия — живые: поддельной
миссии нужны те же методы, что и настоящей (`digestMessage`, `stage`), иначе весь набор падает на
первом раунде. А проверять состав запроса можно только по СНИМКУ (`slice()`): прогон правит тот же
массив в `finally`.

## 8. Оценки

| Работа | Время |
|---|---|
| ~~Часть 36 целиком~~ | **сделано** (3 захода: скриншоты, сбор вывода, остальные четыре куска) |
| ~~Часть 37~~ | **сделано** (`src/lifecycle.js`: старт, порядок остановки, изоляция шагов) |
| Этап B целиком | **закрыт**: в `main.js` только сборка модулей |

Если делать только код и набор, без живых прогонов и негативных контролей, выходит в 2–3 раза быстрее — но именно живой прогон поймал все настоящие находки этого этапа (в части 37 — обрыв цепочки `whenReady` из-за моста с негодным портом).

## 9. Команды

```bash
npm test                                  # 64 набора, 1332 проверки
npm run test:live:lifecycle               # жизненный цикл: мост, дети, порядок остановки
npm run test:live:tool-registry           # и остальные: run-ipc, shell-ipc, projects, app,
                                          # settings, stage, model, browser, github, brief…
npm run test:boot                         # настоящий Electron
node scripts/audit-extractions.js HEAD WORKTREE src/foo.js src/bar.js
npm run ota:key              # один раз: ключ подписи наборов (~/.ai-agent-ota-key.pem)
npm run ota:key -- --trust   # и доверить открытую часть приложению
npm run ota:bundle -- --version 1.5.192   # подписанный набор (без ключа сборка откажет)
```

## 10. Куда смотреть в первую очередь

- хвост `AGENT-NOTES.md` — что делалось по частям и почему;
- `ARCHITECTURE.md` — карта модулей;
- `src/main.js` — место сборки: видно, кто кого собирает и какие значения идут мостом `live`.
